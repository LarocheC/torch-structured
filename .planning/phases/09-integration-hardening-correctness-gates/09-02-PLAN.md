---
phase: 09-integration-hardening-correctness-gates
plan: 02
type: execute
wave: 2
depends_on: ['09-01']
files_modified:
  - torch_structured/_ops.py
  - torch_structured/__init__.py
  - torch_structured/_triton/butterfly/op.py
  - tests/test_torch_compile_triton.py
  - tests/test_distributed_triton.py
  - tests/test_deterministic_mode.py
  - .github/workflows/test.yml
autonomous: true
requirements: [COMPAT-04]
must_haves:
  truths:
    - "torch.compile(Butterfly(...), fullgraph=True)(x).sum().backward() under BACKEND=triton traces without any graph break (raises torch._dynamo.exc.Unsupported if any break occurs); 260419-p27 fake-tensor bug does not re-trigger through the nn.Module surface."
    - "torch.compile(ButterflyBmm), torch.compile(LRU(kind='butterfly')), torch.compile(make_linear('butterfly', ...)) all trace cleanly with fullgraph=True under BACKEND=triton."
    - "Single-process DDP smoke (gloo backend, world_size=1) wraps a model containing Butterfly and runs loss.backward() without errors (D-64c)."
    - "2-GPU FSDP smoke (NCCL, FSDP1 with ignored_modules=[Butterfly]) marked @pytest.mark.multigpu runs forward+backward and loss values agree across ranks within fp32 tolerance (D-64a + D-64b + RESEARCH §1)."
    - "torch.utils.checkpoint.checkpoint(use_reentrant=False) wrapped around Butterfly forward produces gradients matching a non-checkpointed reference within rtol=1e-5/atol=1e-6."
    - "torch_structured.set_deterministic(True/False) toggles the module-level _DETERMINISTIC flag, returns the previous value (D-63 save/restore), and is exported via torch_structured.__init__.py __all__ (D-63c)."
    - "Under set_deterministic(True) OR torch.use_deterministic_algorithms(True), two consecutive backward calls produce bit-identical d_twiddle (no atomicAdd reorder noise — the wrapper-level oracle fallback per D-63a)."
    - ".github/workflows/test.yml extends to add a multigpu CI job gated on vars.ENABLE_GPU_CI per RESEARCH §12 (LANDMINE: `pytest -m multigpu` requires the marker registered in conftest — Plan 09-01 Task 2 did this)."
  artifacts:
    - path: "torch_structured/_ops.py"
      provides: "_DETERMINISTIC flag + set_deterministic() setter + _is_deterministic_mode_active() helper (D-63)"
      contains: "set_deterministic"
    - path: "torch_structured/__init__.py"
      provides: "Export set_deterministic via __all__ (D-63c)"
      contains: "set_deterministic"
    - path: "torch_structured/_triton/butterfly/op.py"
      provides: "Wrapper-level deterministic gate at top of _backward body (D-63a — clones the small-N fallback shape at lines 1377-1383)"
      contains: "_is_deterministic_mode_active"
    - path: "tests/test_torch_compile_triton.py"
      provides: "torch.compile(fullgraph=True) tests + FakeTensorMode 260419-p27 acceptance gate"
      min_lines: 100
    - path: "tests/test_distributed_triton.py"
      provides: "DDP smoke + FSDP smoke (@pytest.mark.multigpu) + gradient checkpoint tests"
      min_lines: 120
    - path: "tests/test_deterministic_mode.py"
      provides: "set_deterministic round-trip + bit-identical d_twiddle + torch.use_deterministic_algorithms composition"
      min_lines: 80
    - path: ".github/workflows/test.yml"
      provides: "Multigpu CI job gated on vars.ENABLE_GPU_CI (RESEARCH §12)"
  key_links:
    - from: "torch_structured/_triton/butterfly/op.py:_backward (top of body, BEFORE the small-N fallback at line 1377)"
      to: "torch_structured._ops._is_deterministic_mode_active()"
      via: "if _is_deterministic_mode_active(): route through torch.autograd.grad(_butterfly_multiply_torch, ...) — same shape as lines 1378-1383"
      pattern: "_is_deterministic_mode_active"
    - from: "tests/test_torch_compile_triton.py::test_butterfly_under_fake_tensor_mode"
      to: "Butterfly(in_size=16).forward(fake_x)"
      via: "FakeTensorMode(allow_non_fake_inputs=True) context manager — verifies Phase 7's register_fake at op.py:1577-1597"
      pattern: "FakeTensorMode"
    - from: "tests/test_distributed_triton.py::test_fsdp_butterfly_smoke (@pytest.mark.multigpu)"
      to: "torch.distributed.fsdp.FullyShardedDataParallel(model, ignored_modules=[m for m in model.modules() if isinstance(m, Butterfly)])"
      via: "FSDP1 NCCL with no-shard hint for Butterfly per D-64b + RESEARCH §1"
      pattern: "FullyShardedDataParallel"
    - from: "torch_structured.__init__.py __all__"
      to: "torch_structured._ops.set_deterministic"
      via: "from ._ops import set_backend, set_deterministic"
      pattern: "set_deterministic"
---

<objective>
Plan 09-02 (Wave 2 — Compose, SC#2): Bring all the cross-cutting integrations that real users hit — `torch.compile`, DDP, FSDP, gradient checkpointing — to a working state on the Triton backend. Add the `set_deterministic()` opt-in API with a wrapper-level oracle fallback in `_backward`. Extend the CI workflow with a multigpu job.

Purpose: With the §0 LANDMINE fixed by Plan 09-01, the nn.Module surface now routes through the Triton kernel under `BACKEND=triton`. This plan exercises the FOUR composability stories users hit: `torch.compile(fullgraph=True)` (resolves 260419-p27 end-to-end), FSDP1 with ignored_modules for the twiddle no-shard hint (RESEARCH §1 — FSDP2 has no ignored_params in PyTorch 2.6), DDP smoke, and gradient checkpointing. Deterministic mode opt-in via `set_deterministic()` routes the backward through the pure-PyTorch oracle (D-63a recommendation), giving bit-identical d_twiddle at the cost of speed.

Output: Three new test files (`test_torch_compile_triton.py`, `test_distributed_triton.py`, `test_deterministic_mode.py`). Two new helpers in `_ops.py` (`set_deterministic`, `_is_deterministic_mode_active`). One export update in `__init__.py`. One wrapper-level gate at the top of `_backward` (`_triton/butterfly/op.py`). One CI workflow extension adding the multigpu job.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-integration-hardening-correctness-gates/09-CONTEXT.md
@.planning/phases/09-integration-hardening-correctness-gates/09-RESEARCH.md
@.planning/phases/09-integration-hardening-correctness-gates/09-PATTERNS.md
@.planning/phases/09-integration-hardening-correctness-gates/09-01-SUMMARY.md
@.planning/phases/08-butterfly-multiply-backward-triton/08-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Extracted from codebase. Executor does not need to re-explore. -->

From `torch_structured/_triton/butterfly/op.py:1315-1383` (current `_backward` head — Phase 9 09-02 prepends a deterministic gate BEFORE the small-N branch at line 1377):
```python
def _backward(ctx, grad_out):
    twiddle, input_ = ctx.saved_tensors
    increasing_stride = ctx.increasing_stride
    output_size = ctx.output_size

    batch_size, nstacks, input_size = input_.shape
    nblocks = twiddle.shape[1]
    log_n = twiddle.shape[2]
    n = 1 << log_n
    is_complex = twiddle.is_complex()

    # D-49b small-N fallback — Phase 9 09-02 prepends the deterministic gate ABOVE this branch
    if log_n <= 1:
        twiddle_d = twiddle.detach().requires_grad_(True)
        input_d = input_.detach().requires_grad_(True)
        with torch.enable_grad():
            out = _butterfly_multiply_torch(twiddle_d, input_d, increasing_stride, output_size)
        gt, gi = torch.autograd.grad(out, [twiddle_d, input_d], grad_out)
        return gt, gi, None, None
    ...
```

From `torch_structured/_ops.py:303-314` (existing setter shape — clone for set_deterministic):
```python
def set_backend(name: str) -> str:
    actual = _resolve(name)
    log.info("torch_structured: backend=%s (set_backend)", actual)
    return actual
```

From `torch_structured/__init__.py:35-46` (current export surface — add set_deterministic to the from-import and to __all__):
```python
from ._ops import set_backend  # noqa: F401

__all__ = [
    'Butterfly', 'ButterflyBmm', 'ButterflyBase4', 'ButterflyUnitary',
    'butterfly_multiply', 'LRU', 'make_linear', 'set_backend', '__version__',
]
```

From RESEARCH §1 (FSDP1 verbatim pattern — PyTorch 2.6 compatible):
```python
from torch.distributed.fsdp import FullyShardedDataParallel as FSDP

torch.distributed.init_process_group(backend='nccl')
rank = torch.distributed.get_rank()
torch.cuda.set_device(rank)
m = nn.Sequential(Butterfly(256, 256, bias=False), nn.Linear(256, 10)).cuda()
ignored = [mod for mod in m.modules() if isinstance(mod, Butterfly)]
m = FSDP(m, ignored_modules=ignored)
```

From RESEARCH §2 (canonical torch.compile fullgraph=True idiom):
```python
m_compiled = torch.compile(m, fullgraph=True)  # raises torch._dynamo.exc.Unsupported on any graph break
y = m_compiled(x)
loss = y.sum()
loss.backward()  # AOT'd backward; covered by the same fullgraph trace
```

From RESEARCH §3 (FakeTensorMode pattern — 260419-p27 acceptance gate):
```python
from torch._subclasses.fake_tensor import FakeTensorMode
with FakeTensorMode(allow_non_fake_inputs=True) as mode:
    fake_x = mode.from_tensor(x)
    fake_y = m(fake_x)
    assert fake_y.shape == x.shape and fake_y.dtype == x.dtype
```

From RESEARCH §6 (gradient checkpointing pattern — use_reentrant=False is the modern path):
```python
import torch.utils.checkpoint as cp
out_cp = cp.checkpoint(lambda inp: m(inp), x, use_reentrant=False)
```
</interfaces>

<tolerances>
- `torch.compile` graph-break detection: `fullgraph=True` (raises on any break — D-67a planner choice; RESEARCH §2 confirms fullgraph=True is the canonical mechanism).
- Gradient checkpoint vs reference: `rtol=1e-5, atol=1e-6` (Phase 7+8 fp32 d_input envelope; RESEARCH §6 notes 1e-4/1e-5 is overcautious — start at 1e-5/1e-6).
- Deterministic mode bit-identity: `torch.equal(gt1, gt2)` (bit-exact required; the oracle fallback runs pure-PyTorch and is deterministic by construction).
- FSDP loss agreement across ranks: `torch.equal(loss_local, loss_all_gathered_rank_i)` for i ∈ {0, ranks-1} (NCCL's all_gather guarantees bit-identical values on each rank for an averaged loss).
</tolerances>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add set_deterministic() API + wrapper-level oracle fallback in _backward (D-63 + D-63a + D-63b + D-63c)</name>
  <files>torch_structured/_ops.py, torch_structured/__init__.py, torch_structured/_triton/butterfly/op.py, tests/test_deterministic_mode.py</files>
  <read_first>
    - .planning/phases/09-integration-hardening-correctness-gates/09-CONTEXT.md §"Deterministic mode opt-in" (D-63, D-63a, D-63b, D-63c)
    - .planning/phases/09-integration-hardening-correctness-gates/09-RESEARCH.md §4 "Deterministic mode" (lines 347-487 — full pattern including CUBLAS_WORKSPACE_CONFIG fixture)
    - .planning/phases/09-integration-hardening-correctness-gates/09-PATTERNS.md §"_ops.py 09-02 addition" (lines 156-192) + §"_triton/butterfly/op.py" (lines 423-469) + §"Pattern 6 Oracle fallback" (lines 1053-1067)
    - torch_structured/_ops.py:303-314 (set_backend — the shape to clone for set_deterministic)
    - torch_structured/_triton/butterfly/op.py:1315-1390 (current _backward head — Task 1 prepends the deterministic gate ABOVE the small-N branch)
    - torch_structured/__init__.py:1-47 (entire file — modify the from-import and __all__)
    - tests/test_dispatch.py:17-30 (set_backend round-trip test — analog for the deterministic round-trip test)
  </read_first>
  <behavior>
    - Test 1 (`test_set_deterministic_round_trip`): `set_deterministic(True)` returns the previous value (False); `set_deterministic(False)` returns the previous value (True); the module-level flag `_ops._DETERMINISTIC` mirrors the last set value.
    - Test 2 (`test_set_deterministic_exported_from_top_level`): `assert callable(torch_structured.set_deterministic)`; `assert 'set_deterministic' in torch_structured.__all__`.
    - Test 3 (`test_is_deterministic_mode_active_or_semantics`, parametrized over (det_flag, torch_global) ∈ {(F,F), (T,F), (F,T), (T,T)}): `_is_deterministic_mode_active()` returns `det_flag OR torch_global` (D-63b additive OR).
    - Test 4 (`test_deterministic_dtwiddle_bit_identical`, fixture uses CUBLAS_WORKSPACE_CONFIG=:4096:8 — RESEARCH §4 LANDMINE): under `set_deterministic(True)` AND `BACKEND=triton`, two consecutive `loss.backward()` calls on the SAME (twiddle, x, grad_out) produce `torch.equal(gt1, gt2) AND torch.equal(gi1, gi2)`. Use the parameters from RESEARCH §4 lines 433-447 (twiddle shape (1, 1, 9, 256, 2, 2), x shape (4096, 1, 512), fp32, device=cuda — log_n=9 so the kernel path is exercised, not the small-N fallback).
    - Test 5 (`test_deterministic_honors_torch_global_flag`, with `monkeypatch.setenv('CUBLAS_WORKSPACE_CONFIG', ':4096:8')`): `set_deterministic(False)` + `torch.use_deterministic_algorithms(True, warn_only=True)` produces bit-identical d_twiddle across two backward calls (D-63b — the global flag activates the oracle even when the library flag is False).
    - Test 6 (`test_deterministic_non_det_differs_above_epsilon`): under `set_deterministic(False)` + `torch.use_deterministic_algorithms(False)` AND batch=4096 AND log_n=11 (high-atomic-pressure cell), two consecutive backward calls produce d_twiddle that differs ABOVE machine epsilon — proves the deterministic gate actually does something. Use `assert not torch.equal(gt1, gt2)` AND `assert (gt1 - gt2).abs().max() < 1e-2` (sanity bound — proves the diff is real noise, not nonsense).
  </behavior>
  <action>
**Part A — `torch_structured/_ops.py` adds `_DETERMINISTIC` + `set_deterministic` + `_is_deterministic_mode_active`** per D-63 + PATTERNS lines 156-192.

Insert AFTER `set_backend` (after line 314). Body per PATTERNS:
- Module-level `_DETERMINISTIC: bool = False`
- `_is_deterministic_mode_active() -> bool`: returns `_DETERMINISTIC or torch.are_deterministic_algorithms_enabled()` (D-63b additive OR). One-line body.
- `set_deterministic(value: bool) -> bool`: save/restore shape mirroring `set_backend` (D-63 — returns the PREVIOUS value, NOT the new one, per the save/restore pattern). Docstring cites D-63 + D-63a (route through oracle) + D-63b (composition).

**Part B — `torch_structured/__init__.py` exports `set_deterministic`** per D-63c.

Edit line 35: change `from ._ops import set_backend  # noqa: F401` → `from ._ops import set_backend, set_deterministic  # noqa: F401`.

Edit `__all__` (lines 37-47): insert `'set_deterministic',` after `'set_backend',`.

**Part C — `torch_structured/_triton/butterfly/op.py` adds the wrapper-level gate at top of `_backward`** per D-63a + PATTERNS lines 446-468.

Insert the deterministic gate IMMEDIATELY AFTER the `is_complex = twiddle.is_complex()` line (currently at line 1372) and BEFORE the small-N fallback (currently at line 1377). The gate clones the small-N fallback shape verbatim — same `twiddle_d.detach().requires_grad_(True)` + `with torch.enable_grad():` + `torch.autograd.grad` body.

Specific edits:
- Local import inside `_backward`: `from torch_structured._ops import _is_deterministic_mode_active`. Local-scoped to avoid module-import cycle (`_ops.py` imports from `_triton` for the resolver; the resolver doesn't need the deterministic helper). Comment cites D-63a.
- The gate: `if _is_deterministic_mode_active(): twiddle_d = twiddle.detach().requires_grad_(True); input_d = input_.detach().requires_grad_(True); with torch.enable_grad(): out = _butterfly_multiply_torch(twiddle_d, input_d, increasing_stride, output_size); gt, gi = torch.autograd.grad(out, [twiddle_d, input_d], grad_out); return gt, gi, None, None`. EXACTLY the same body as the small-N fallback at lines 1378-1383, just a different predicate.
- Inline comment: "Phase 9 D-63a: wrapper-level deterministic gate. When set_deterministic(True) OR torch.use_deterministic_algorithms(True) is active (D-63b additive OR), route through the pure-PyTorch oracle — deterministic by virtue of having no atomicAdd reordering. Identical body shape to the small-N fallback at lines 1377-1383."

NO `@torch.jit.script` (the existing `_backward` body has none — preserve).

**Part D — `tests/test_deterministic_mode.py`** (new file). Per PATTERNS §"tests/test_deterministic_mode.py" (lines 765-799).

Module setup:
- Docstring (Phase 9 09-02 deterministic mode — D-63 contract + D-63a oracle fallback verification)
- `pytestmark = pytest.mark.skipif(not torch.cuda.is_available(), reason="deterministic mode tests require CUDA")` per PATTERNS Pattern 4
- Imports: `pytest`, `torch`, `torch_structured`, `from torch_structured import Butterfly`

Fixtures:
- `deterministic_env(monkeypatch)`: sets `CUBLAS_WORKSPACE_CONFIG=:4096:8` per RESEARCH §4 LANDMINE (lines 474-482). Used by Tests 4-6 to avoid spurious cublas errors when `torch.use_deterministic_algorithms(True)` is set.

Test bodies — translate the 6 behaviors above into concrete tests. For Tests 4-6, use the parameters from RESEARCH §4 lines 433-447 verbatim (twiddle shape (1, 1, 9, 256, 2, 2), input shape (4096, 1, 512), fp32 — large enough that the atomic-add path is exercised at log_n=9, small enough to fit on an 8GB dev GPU).

For Test 6 ("non-det differs above epsilon"): use log_n=11 + batch=4096 (the highest-atomic-pressure cell from the Phase 8 baseline) — this is where Phase 8 D-52 documented the d_twiddle atomicAdd noise envelope (rtol=1e-3/atol=1e-4 at batch=4096). The non-det path SHOULD differ at this cell; if `torch.equal(gt1, gt2)` holds even with `set_deterministic(False)`, the deterministic gate is doing nothing (the test fails with a clear message).

Quality gates after Task 1:
- `python -c "import torch_structured; assert hasattr(torch_structured, 'set_deterministic'); assert 'set_deterministic' in torch_structured.__all__; print(torch_structured.set_deterministic(False))"` prints `False`.
- `grep -nE 'def set_deterministic\b' torch_structured/_ops.py | grep -v '^[^:]*:[^:]*:#'` returns 1 line.
- `grep -c '_is_deterministic_mode_active' torch_structured/_triton/butterfly/op.py` returns ≥ 1 (the local import + the if-check).
- `pytest tests/test_deterministic_mode.py -xvs --no-header` passes all 6 tests.
- `pytest tests/test_butterfly_triton.py -k 'not slow' -xvs --no-header` STILL PASSES (the new gate is dormant when `_DETERMINISTIC=False` and `torch.are_deterministic_algorithms_enabled()=False` — the default).
  </action>
  <verify>
    <automated>pytest tests/test_deterministic_mode.py -xvs --no-header</automated>
    <grep>grep -cE 'def set_deterministic\b' torch_structured/_ops.py</grep>
    <expect>1</expect>
    <grep>grep -c '_is_deterministic_mode_active' torch_structured/_triton/butterfly/op.py</grep>
    <expect>≥ 2 (one import line + one if-check)</expect>
    <grep>grep -c "'set_deterministic'" torch_structured/__init__.py</grep>
    <expect>≥ 1 (in __all__)</expect>
  </verify>
  <done>
    - `set_deterministic`, `_DETERMINISTIC`, `_is_deterministic_mode_active` defined in `_ops.py`; save/restore semantics; D-63b additive OR with `torch.are_deterministic_algorithms_enabled()`.
    - `torch_structured/__init__.py` exports `set_deterministic` via `from ._ops import` AND `__all__`.
    - `_backward` in `_triton/butterfly/op.py` has the wrapper-level gate above the small-N fallback.
    - 6 deterministic-mode tests pass; Phase 7+8 regression check still passes (the gate is dormant by default).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: torch.compile(fullgraph=True) + FakeTensorMode tests in tests/test_torch_compile_triton.py (resolves 260419-p27)</name>
  <files>tests/test_torch_compile_triton.py</files>
  <read_first>
    - .planning/phases/09-integration-hardening-correctness-gates/09-CONTEXT.md §"In scope" 09-02 torch.compile bullets (lines 25-31)
    - .planning/phases/09-integration-hardening-correctness-gates/09-RESEARCH.md §2 "torch.compile graph-break detection" (lines 213-260) + §3 "260419-p27 end-to-end" (lines 263-343)
    - .planning/phases/09-integration-hardening-correctness-gates/09-PATTERNS.md §"tests/test_torch_compile_triton.py" (lines 707-749)
    - .planning/phases/04-triton-dispatch-infrastructure-foundational-decisions/04-CONTEXT.md D-13..D-14 (the 260419-p27 acceptance gate that Phase 4 originally lit up via the demonstrator op)
    - torch_structured/_triton/butterfly/op.py:1577-1597 (the `register_fake` body — verifies the meta kernel returns correctly-shaped tensors under FakeTensorMode)
    - torch_structured/butterfly/butterfly.py (Butterfly class — verifies the §0 LANDMINE fix from Plan 09-01 took effect: the nn.Module surface now routes through `_ops`)
    - .planning/phases/09-integration-hardening-correctness-gates/09-01-SUMMARY.md (specifically: did the LANDMINE fix land cleanly? Did monkey-patch tests survive?)
  </read_first>
  <behavior>
    - Test 1 (`test_torch_compile_butterfly_fullgraph_no_break`, `@pytest.mark.op('butterfly_multiply')`, only runs when backend=='triton'): `torch.compile(Butterfly(in_size=256, out_size=256, bias=False, complex=False).cuda(), fullgraph=True)(x).sum().backward()` raises NO exception. Verifies fullgraph=True + the autograd-AOT'd backward both trace through the Triton kernel without graph break.
    - Test 2 (`test_torch_compile_butterfly_small_n_branch_no_break`, `@pytest.mark.op('butterfly_multiply')`): same as Test 1 but `in_size=16` (log_n=4 — well above the small-N fallback at log_n<=1). RESEARCH §2 LANDMINE: `fullgraph=True` handles small-N as a guard, not a runtime break, but we should verify BOTH dense paths just in case.
    - Test 3 (`test_torch_compile_butterfly_bmm_fullgraph_no_break`, `@pytest.mark.op('butterfly_multiply')`): `torch.compile(ButterflyBmm(in_size=256, out_size=256, matrix_batch=4, bias=False).cuda(), fullgraph=True)(x).sum().backward()` traces cleanly.
    - Test 4 (`test_torch_compile_lru_butterfly_fullgraph_no_break`, `@pytest.mark.op('butterfly_multiply')`): `torch.compile(LRU(input_size=64, hidden_size=64, num_layers=1, batch_first=True, kind='butterfly').cuda(), fullgraph=True)(x)` traces cleanly. The `kind='butterfly'` routes LRU's B and C projections through butterfly; the test verifies the trace survives the complex-state scan boundary.
    - Test 5 (`test_torch_compile_make_linear_butterfly_fullgraph_no_break`, `@pytest.mark.op('butterfly_multiply')`): `torch.compile(make_linear('butterfly', 256, 256).cuda(), fullgraph=True)(x).sum().backward()` traces cleanly.
    - Test 6 (`test_butterfly_under_fake_tensor_mode`, `@pytest.mark.op('butterfly_multiply')`): wrap Butterfly forward call in `FakeTensorMode(allow_non_fake_inputs=True)`; verify `fake_y.shape == x.shape and fake_y.dtype == x.dtype`. This is the 260419-p27 acceptance gate — verifies Phase 7's `register_fake` at `_triton/butterfly/op.py:1577-1597` works end-to-end through the §0-fixed nn.Module surface.
    - Test 7 (`test_butterfly_register_fake_is_present`): regression detector — `from torch_structured._triton.butterfly.op import butterfly_multiply as t_op` then `assert hasattr(t_op, '_abstract_fn') or hasattr(t_op, '_fake_fn') or hasattr(t_op, '_meta_fn')` (the exact attribute name varies by PyTorch version; the test accepts any of the documented variants — RESEARCH §3 line 333). Detects regressions where register_fake gets accidentally removed in a future refactor.
  </behavior>
  <action>
Create `tests/test_torch_compile_triton.py` per PATTERNS lines 713-749.

Module setup:
- Docstring: "Phase 9 09-02 torch.compile + FakeTensorMode integration tests."
- `pytestmark = pytest.mark.skipif(not torch.cuda.is_available(), reason="torch.compile tests require CUDA")` per PATTERNS Pattern 4
- Imports: `pytest`, `torch`, `from torch._subclasses.fake_tensor import FakeTensorMode`, `import torch_structured`, `from torch_structured import Butterfly, ButterflyBmm, LRU, make_linear`

For Tests 1-5, the body shape is identical:
```
@pytest.mark.op('butterfly_multiply')
def test_torch_compile_<variant>_fullgraph_no_break(backend):
    if backend != "triton":
        pytest.skip("torch.compile no-break gate is meaningful only for triton path")
    m = <variant_constructor>.cuda()
    m_compiled = torch.compile(m, fullgraph=True)
    x = <variant_input>
    y = m_compiled(x)
    loss = y.sum()
    loss.backward()  # AOT'd backward — fullgraph=True covers per RESEARCH Pitfall 3
    assert x.grad is not None and torch.isfinite(x.grad).all()
```

Per RESEARCH §2 Pitfall 3 ("`fullgraph=True` misses backward-only graph breaks"): ALWAYS call `loss.backward()` — not just forward. The test verifies BOTH directions.

For Test 6 (FakeTensorMode end-to-end), per RESEARCH §3 lines 285-303 verbatim — use `mode.from_tensor(x)` (not direct fake construction), wrap in `with FakeTensorMode(allow_non_fake_inputs=True) as mode:`. Documents the 260419-p27 bug context in the docstring.

For Test 7 (register_fake regression detector), the exact attribute name on a `triton_op` object varies — try `_abstract_fn`, `_fake_fn`, `_meta_fn`, or `hasattr(t_op, 'register_fake')`. Document: "The exact attribute name depends on PyTorch version; this is a regression detector — if NONE of the candidate attributes are present, register_fake was accidentally removed."

NOTE on the LRU test (Test 4): LRU's hidden state is `complex64` per `_LRULayer.forward`. The butterfly projections are REAL (kind_B='butterfly' builds Butterfly(complex=False) — see RESEARCH §10 LANDMINE). The trace must survive the `gamma * torch.complex(b_re, b_im)` boundary; if this raises a complex-related dynamo error, the test fails with a clear message and the executor surfaces this as a follow-up issue (LRU + torch.compile may need additional FakeTensorMode coverage in a future phase).

Quality gates after Task 2:
- 7 tests exist (5 fullgraph + 1 FakeTensorMode + 1 register_fake regression).
- All run under `backend='triton'` (skip torch/cuda parametrize values per the explicit `if backend != "triton": pytest.skip()` guard).
- `pytest tests/test_torch_compile_triton.py -xvs --no-header` passes (skipping torch/cuda axes).
  </action>
  <verify>
    <automated>pytest tests/test_torch_compile_triton.py -xvs --no-header</automated>
    <grep>grep -cE 'def test_torch_compile_\w+_fullgraph_no_break' tests/test_torch_compile_triton.py</grep>
    <expect>≥ 4 (butterfly + ButterflyBmm + LRU + make_linear — small_n variant counts separately)</expect>
    <grep>grep -c 'fullgraph=True' tests/test_torch_compile_triton.py</grep>
    <expect>≥ 5 (one per fullgraph test)</expect>
    <grep>grep -c 'FakeTensorMode' tests/test_torch_compile_triton.py</grep>
    <expect>≥ 2 (import + at least one use)</expect>
  </verify>
  <done>
    - `tests/test_torch_compile_triton.py` ships with 7 tests covering Butterfly, ButterflyBmm, LRU, make_linear under `torch.compile(fullgraph=True)` AND the FakeTensorMode 260419-p27 end-to-end gate.
    - Every fullgraph test calls BOTH forward AND backward (per RESEARCH Pitfall 3).
    - register_fake regression detector defends against accidental removal.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DDP + FSDP + gradient checkpointing tests in tests/test_distributed_triton.py + multigpu CI job</name>
  <files>tests/test_distributed_triton.py, .github/workflows/test.yml</files>
  <read_first>
    - .planning/phases/09-integration-hardening-correctness-gates/09-CONTEXT.md §"In scope" 09-02 distributed bullets (lines 32-35) + §"FSDP test execution" (D-64, D-64a, D-64b, D-64c)
    - .planning/phases/09-integration-hardening-correctness-gates/09-RESEARCH.md §1 "PyTorch 2.6+ FSDP API" (lines 144-210) + §6 "torch.utils.checkpoint" (lines 555-613) + §12 "CI workflow + multigpu job design" (lines 1029-1102)
    - .planning/phases/09-integration-hardening-correctness-gates/09-PATTERNS.md §"tests/test_distributed_triton.py" (lines 753-762) + §".github/workflows/test.yml" (lines 928-967)
    - tests/conftest.py (verify Task 2 of Plan 09-01 registered the `multigpu` marker — required for `pytest -m multigpu` to work; RESEARCH §12 Pitfall: unregistered markers cause `pytest -m multigpu` to run NOTHING)
    - .github/workflows/test.yml (current 48-line CPU-only workflow — extend per RESEARCH §12)
  </read_first>
  <behavior>
    - Test 1 (`test_ddp_butterfly_smoke`, `@pytest.mark.op('butterfly_multiply')`): single-process DDP (D-64c) — `torch.distributed.init_process_group(backend='gloo', init_method='env://', rank=0, world_size=1, store=torch.distributed.HashStore())`. Wrap `nn.Sequential(Butterfly(256, 256, bias=False), nn.Linear(256, 10))` with `DistributedDataParallel`. Run loss.backward(). Verify gradient sync succeeds (the gradient norms are positive). Skip when `torch.distributed.is_available() is False`. Destroy process group in finally. Run under `BACKEND=triton` only (`if backend != "triton": pytest.skip()`).
    - Test 2 (`test_fsdp_butterfly_smoke`, `@pytest.mark.multigpu` + `@pytest.mark.op('butterfly_multiply')`): 2-GPU FSDP (D-64 + D-64b + RESEARCH §1) — `torch.distributed.init_process_group('nccl')`, `torch.cuda.set_device(rank)`. Model = `nn.Sequential(Butterfly(256, 256, bias=False), nn.Linear(256, 10)).cuda()`. Wrap with `FSDP(m, ignored_modules=[m for m in m.modules() if isinstance(m, Butterfly)])` per D-64b. Forward + backward on `torch.randn(8, 256, device='cuda')`. Verify `torch.distributed.barrier()` returns + loss values agree across ranks within fp32 tolerance (collect via `all_gather` into a tensor and compare). Cleanup with `destroy_process_group()`. NOTE: this test ONLY runs via `torchrun --nproc_per_node=2`; under plain pytest it's skipped via the multigpu marker.
    - Test 3 (`test_gradient_checkpointing_butterfly`, `@pytest.mark.op('butterfly_multiply')`): per RESEARCH §6 lines 581-605. `torch.utils.checkpoint.checkpoint(lambda inp: m(inp), x, use_reentrant=False)` wrapped around a Butterfly forward. Compare gradients to a non-checkpointed reference within `rtol=1e-5, atol=1e-6` (RESEARCH §6 — start tight; loosen to 1e-4/1e-5 only if it actually mismatches in practice). Run under `BACKEND=triton`. Use `m.twiddle.grad` (direct Butterfly, NOT make_linear — see RESEARCH §6 LANDMINE about `m.b.twiddle.grad` indirection).
    - Behavioral assertion for CI workflow: `.github/workflows/test.yml` has at least 2 jobs after Task 3 (the existing `test` job + a new `test-multigpu` job gated on `vars.ENABLE_GPU_CI`).
  </behavior>
  <action>
**Part A — `tests/test_distributed_triton.py`** (new file). Per PATTERNS lines 753-762 + RESEARCH §1 + §6.

Module setup:
- Docstring: "Phase 9 09-02 DDP/FSDP/gradient-checkpointing integration tests. FSDP test is @pytest.mark.multigpu (requires torchrun --nproc_per_node=2)."
- `pytestmark = pytest.mark.skipif(not torch.cuda.is_available(), reason="distributed tests require CUDA")` per PATTERNS Pattern 4
- Imports: `os`, `pytest`, `torch`, `torch.nn as nn`, `import torch_structured`, `from torch_structured import Butterfly`, `import torch.utils.checkpoint as cp`

Test 1 (DDP single-process smoke per D-64c). Per RESEARCH §1.5: use `gloo` backend (CPU collectives — exercises the DDP wrap path without requiring 2 GPUs). `world_size=1, rank=0`. Set env vars `MASTER_ADDR=localhost`, `MASTER_PORT='<unique>'` via monkeypatch or `os.environ.setdefault`. Wrap model with DDP. Forward + backward. Cleanup `destroy_process_group`.

Test 2 (FSDP 2-GPU smoke per D-64 + RESEARCH §1 lines 167-198 VERBATIM). The test body is the FSDP1 pattern from RESEARCH §1; only runs under torchrun, marked `@pytest.mark.multigpu`. Docstring documents: "Run via: torchrun --nproc_per_node=2 -m pytest tests/test_distributed_triton.py -m multigpu -v". The PyTorch 2.6 baseline drives the choice of FSDP1 (FSDP2 lacks `ignored_params` in 2.6 — RESEARCH §1 verified).

The FSDP test runs under torchrun and reads `WORLD_SIZE`, `RANK`, `LOCAL_RANK` from env vars; under plain `pytest` these are unset and the multigpu marker handles the skip. Add an internal sanity assert at test entry: `assert int(os.environ.get('WORLD_SIZE', 1)) >= 2, "FSDP test requires torchrun --nproc_per_node=2"`.

Test 3 (gradient checkpointing per RESEARCH §6 lines 581-605 VERBATIM). Important: use a DIRECT `Butterfly(256, 256, ...)` instance, NOT `make_linear(...)` — see RESEARCH §6 LANDMINE about `m.b.twiddle.grad` vs `m.twiddle.grad`. Tolerance: start at rtol=1e-5/atol=1e-6 per RESEARCH §6 recommendation; if the test fails in execution, the executor can loosen to 1e-4/1e-5 and document why in 09-02-SUMMARY.md.

For all three tests: `if backend != "triton": pytest.skip("distributed integration is meaningful only for triton path")`.

**Part B — `.github/workflows/test.yml`** extension per RESEARCH §12 lines 1054-1093.

Read current workflow file. ADD (do not replace) two job entries below the existing `test` job:

1. `test-triton` (new): `runs-on: [self-hosted, gpu, ampere]` (CONTEXT.md doesn't lock the runner label; planner picks per RESEARCH §12 — recommend `[self-hosted, gpu, ampere]`). Gated on `if: ${{ vars.ENABLE_GPU_CI == 'true' }}` per RESEARCH §12 Q2 (the project may not yet have GPU CI; the workflow ships the entries gated by the repo variable). Env: `TORCH_STRUCTURED_BACKEND: triton`. Runs `pytest tests/ -m "not multigpu" -v`. SHARES the Triton cache key with the existing `test` job (same `hashFiles('torch_structured/_triton/**/*.py')`).

2. `test-multigpu` (new): `runs-on: [self-hosted, gpu, multi-gpu]`. Gated on `if: ${{ vars.ENABLE_MULTIGPU_CI == 'true' }}`. `needs: test-triton`. Runs `torchrun --nproc_per_node=2 -m pytest tests/test_distributed_triton.py -v -m multigpu`. SHARES the Triton cache key.

ALSO extend the existing `test` job's pytest command: change `pytest tests/test_dispatch.py -v` to `pytest tests/test_dispatch.py tests/test_phase9_integration.py -v -m "not multigpu and not slow"` so the CPU job covers Plan 09-01's new tests too (the dispatch + integration smoke; not the GPU-heavy backend agreement tests which require CUDA). Per RESEARCH §12 Pitfall: explicit `-m "not multigpu"` is REQUIRED to keep multigpu tests out of single-process CI.

Per the marker registration: Plan 09-01 Task 2 already added `multigpu` to `pytest_configure` in `conftest.py`. Verify the registration is present before this task starts (if absent, this task DEPENDS on Plan 09-01 Task 2 — depends_on already encodes this via wave 1 → wave 2).

Quality gates:
- `tests/test_distributed_triton.py` has 3 test functions.
- `grep -c '@pytest.mark.multigpu' tests/test_distributed_triton.py` returns ≥ 1.
- `.github/workflows/test.yml` has ≥ 3 job entries (existing `test` + new `test-triton` + new `test-multigpu`).
- `pytest tests/test_distributed_triton.py::test_ddp_butterfly_smoke tests/test_distributed_triton.py::test_gradient_checkpointing_butterfly -xvs --no-header` passes (the multigpu test is skipped under plain pytest — that's expected).
  </action>
  <verify>
    <automated>pytest tests/test_distributed_triton.py -xvs --no-header -m "not multigpu"</automated>
    <grep>grep -c '@pytest.mark.multigpu' tests/test_distributed_triton.py</grep>
    <expect>≥ 1</expect>
    <grep>grep -cE '^  test-(triton|multigpu):' .github/workflows/test.yml</grep>
    <expect>2 (test-triton + test-multigpu jobs added)</expect>
    <grep>grep -c 'pytest tests/test_dispatch.py' .github/workflows/test.yml</grep>
    <expect>1 (the existing test job's command was extended, not duplicated)</expect>
  </verify>
  <done>
    - `tests/test_distributed_triton.py` has 3 tests (DDP, FSDP, checkpoint).
    - FSDP test marked `@pytest.mark.multigpu` and uses FSDP1 with `ignored_modules=[...]` per D-64b + RESEARCH §1.
    - DDP smoke test uses gloo single-process world_size=1 per D-64c.
    - Gradient checkpointing test uses `use_reentrant=False` per RESEARCH §6.
    - `.github/workflows/test.yml` extended with `test-triton` and `test-multigpu` jobs gated on `vars.ENABLE_*_CI` per RESEARCH §12.
    - All non-multigpu tests pass under plain pytest; multigpu tests skip cleanly (run via torchrun in CI when `vars.ENABLE_MULTIGPU_CI == 'true'`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Python code → torch_structured.set_deterministic() | User passes a bool. Cast via `bool(value)` defensively. No state machine. |
| Multiprocess torchrun → distributed FSDP test | torchrun sets RANK/WORLD_SIZE/MASTER_ADDR env vars; the test reads them. No untrusted input — torchrun is invoked by CI. |
| Plug-in execution `torch.compile` → `register_fake` body | `register_fake` returns `torch.empty(...)` with shapes derived from the user-supplied input tensor; trusted code path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | T (Tampering) | `set_deterministic` flag mutation | accept | Module-level `_DETERMINISTIC` is a Python global; mutation is the documented API. Thread-safety: not a concern for the project's single-threaded test/training use case. |
| T-09-07 | E (Elevation of privilege) | `torch.utils.checkpoint(use_reentrant=False)` closure capture | accept | The closure captures the Butterfly module's parameters at test setup; no untrusted code reaches the closure body. |
| T-09-08 | D (Denial of service) | Multigpu CI job consuming GPU resources | mitigate | Gated on `vars.ENABLE_MULTIGPU_CI == 'true'` — explicit opt-in by repo admin. Self-hosted GPU runner is rate-limited by the project's allotment, not exposed to untrusted PRs without runner approval. |
| T-09-09 | I (Information disclosure) | `loss` values logged across ranks in FSDP test | accept | Loss values are derived from `torch.randn(...)` — synthetic test inputs; no sensitive data. |
| T-09-10 | S (Spoofing) | gloo init_method='env://' in DDP test | accept | Single-process world_size=1 with HashStore — no network exposure. |
</threat_model>

<verification>
- `pytest tests/test_deterministic_mode.py tests/test_torch_compile_triton.py tests/test_distributed_triton.py -xvs --no-header -m "not multigpu"` exits 0.
- `pytest tests/test_butterfly_triton.py -k "not slow" -xvs --no-header` STILL PASSES — Phase 7+8 regression check (the new gate is dormant by default; the §0 LANDMINE fix from Plan 09-01 + this plan's additions to `_backward` do not break existing behavior).
- `pytest tests/test_phase9_integration.py -xvs --no-header` STILL PASSES (Plan 09-01 tests).
- `.github/workflows/test.yml` lints cleanly (`yamllint -d "rules: {line-length: disable}"` or similar — non-blocking sanity check).
</verification>

<success_criteria>
- SC#2 (compose): All four composability stories (`torch.compile`, DDP, FSDP, checkpoint) light up under `BACKEND=triton` with the §0 LANDMINE fixed.
- 260419-p27 end-to-end: `torch.compile(Butterfly, fullgraph=True)` traces without graph break + `FakeTensorMode` returns correctly-shaped tensors.
- COMPAT-04: `torch.compile(model)` works for Butterfly / ButterflyBmm / LRU / make_linear.
- D-63 deterministic mode: `set_deterministic` API ships; oracle fallback gates `_backward` at the wrapper level; composes additively with `torch.use_deterministic_algorithms`.
- D-64 multigpu CI: workflow shipped (gated on repo variable until GPU CI is provisioned).
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-hardening-correctness-gates/09-02-SUMMARY.md` documenting:
  - Which torch.compile fullgraph tests passed end-to-end vs which surfaced unexpected dynamo behavior (e.g., LRU + torch.compile may need follow-up)
  - Whether the gradient-checkpoint tolerance held at rtol=1e-5/atol=1e-6 or needed loosening to 1e-4/1e-5
  - Whether the FSDP test was actually exercised on a 2-GPU runner (in CI vs locally) or remained a written-but-unexercised gate
  - Any unexpected interactions between the deterministic-mode gate and Phase 8 SC#4 (the gate is BEFORE the small-N branch — confirm SC#4 test still passes)
</output>
