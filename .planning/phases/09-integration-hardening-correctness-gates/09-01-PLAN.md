---
phase: 09-integration-hardening-correctness-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - torch_structured/butterfly/multiply.py
  - torch_structured/_ops.py
  - tests/conftest.py
  - tests/test_phase9_integration.py
autonomous: true
requirements: [TEST-03, TEST-06, COMPAT-01, COMPAT-02, COMPAT-03]
must_haves:
  truths:
    - "Butterfly(in_size=256).forward(x) under set_backend('triton') invokes the Triton kernel, NOT torch.ops.torch_structured.butterfly_multiply (C++ op) — the §0 LANDMINE is fixed at multiply.py:27-30."
    - "tests/conftest.py backend fixture parametrizes ['torch', 'triton', 'cuda'] with per-test cuda skip-gate via @pytest.mark.op('<op_name>') markers."
    - "Backend-agreement tests for butterfly_multiply, diag_mult, hadamard_transform pass under all 3 backends at Phase 7+8 tolerances (fp32 rtol=1e-5/atol=1e-6; complex64 d_twiddle rtol=1e-3/atol=1e-4; complex64 d_input rtol=1e-5/atol=1e-6)."
    - "A v1.0/v1.1-style state_dict (twiddle shape (nstacks, nblocks, log_n, n/2, 2, 2)) saved by torch.save round-trips through Butterfly.load_state_dict under BACKEND=triton, producing forward outputs identical to BACKEND=cuda within fp32 tolerance."
    - "make_linear('butterfly', 256, 256) and LRU(input_size=64, hidden_size=64, kind='butterfly') forward+backward succeed under BACKEND=triton with finite gradients."
    - "Public API regression detector via inspect.signature snapshot fails when Butterfly / ButterflyBmm / ButterflyUnitary / ButterflyBase4 .__init__ signature changes."
    - "TORCH_STRUCTURED_BACKEND=triton pytest tests/ -m 'not multigpu and not slow' exits 0 end-to-end (TEST-06)."
  artifacts:
    - path: "torch_structured/butterfly/multiply.py"
      provides: "D-05 attribute-access delegators replacing the C++ direct calls (the §0 LANDMINE fix)"
      contains: "torch_structured._ops.butterfly_multiply"
    - path: "torch_structured/_ops.py"
      provides: "_has_cuda_legacy_for_op(op_name) per-op probe"
      contains: "_has_cuda_legacy_for_op"
    - path: "tests/conftest.py"
      provides: "3-axis backend fixture + multigpu/op marker registration"
      contains: "params=[\"torch\", \"triton\", \"cuda\"]"
    - path: "tests/test_phase9_integration.py"
      provides: "Backend-agreement + checkpoint round-trip + make_linear/LRU + public API tests"
      min_lines: 150
  key_links:
    - from: "torch_structured/butterfly/butterfly.py:124,128,239,243"
      to: "torch_structured._ops.butterfly_multiply"
      via: "from .multiply import butterfly_multiply -> Phase 9 delegator -> _ops attribute access"
      pattern: "torch_structured\\._ops\\.butterfly_multiply"
    - from: "tests/conftest.py backend fixture (cuda axis)"
      to: "_ops._has_cuda_legacy_for_op(op_name)"
      via: "@pytest.mark.op('<op_name>') marker on each parametrized test"
      pattern: "_has_cuda_legacy_for_op"
    - from: "tests/test_phase9_integration.py test_checkpoint_v10_v11_roundtrip"
      to: "Butterfly.load_state_dict under BACKEND=triton"
      via: "torch.save(state_dict) -> torch.load -> load_state_dict; compare forward output across BACKEND=triton vs BACKEND=cuda"
      pattern: "load_state_dict"
---

<objective>
Plan 09-01 (Wave 1 — Foundations, SC#1 + SC#3): Fix the §0 LANDMINE that bypasses `_ops` for every nn.Module call. Widen the `backend` fixture to a third axis (`cuda`) with per-op skip-gate. Add backend-agreement tests, v1.0/v1.1 checkpoint round-trip, `make_linear` + `LRU` smoke tests, and a public API regression detector. Verify the existing test suite passes under `TORCH_STRUCTURED_BACKEND=triton`.

Purpose: Phase 7+8 shipped the Triton kernels but `Butterfly(...).forward(x)` STILL routes through the C++ op because `from .multiply import butterfly_multiply` binds at import time to a wrapper that calls `torch.ops.torch_structured.butterfly_multiply` directly. Without the §0 fix, every Phase 9 downstream test (torch.compile, FSDP, DDP, checkpoint) passes green BUT verifies the C++ path — Phase 9's deliverable would ship a user-visible bug: `set_backend('triton')` has no effect on `Butterfly`. Plan 09-01 makes the nn.Module surface honor backend selection, then exercises the rest of SC#1 + SC#3.

Output: `torch_structured/butterfly/multiply.py` rewrites the three top-level callables as D-05 attribute-access delegators (verbatim template from `structured/hadamard.py:25-34`). `tests/conftest.py` widens to 3-axis with `@pytest.mark.op('<op_name>')` markers and registers `multigpu`/`op` markers. `torch_structured/_ops.py` adds `_has_cuda_legacy_for_op(op_name)`. `tests/test_phase9_integration.py` (new) bundles ~7 tests covering all of SC#1 + SC#3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-integration-hardening-correctness-gates/09-CONTEXT.md
@.planning/phases/09-integration-hardening-correctness-gates/09-RESEARCH.md
@.planning/phases/09-integration-hardening-correctness-gates/09-PATTERNS.md
@.planning/phases/08-butterfly-multiply-backward-triton/08-01-SUMMARY.md
@.planning/phases/08-butterfly-multiply-backward-triton/08-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase by the planner. -->
<!-- No codebase exploration needed — these are the source-of-truth surfaces this plan touches. -->

From `torch_structured/structured/hadamard.py:20-34` (VERBATIM D-05 delegator template — copy this shape into `butterfly/multiply.py`):
```python
import torch_structured  # noqa: F401 — needed so the shim below can attribute-access _ops

from torch_structured._torch_ref.hadamard import hadamard_transform_torch  # noqa: F401 — back-compat shim per D-33d


def hadamard_transform(*args, **kwargs):
    """Back-compat shim — delegates to ``torch_structured._ops.hadamard_transform``."""
    return torch_structured._ops.hadamard_transform(*args, **kwargs)
```

From `torch_structured/butterfly/multiply.py:1-31` (CURRENT state — the LANDMINE; replace with delegators):
```python
@torch.jit.script
def butterfly_multiply_fw(twiddle, input, increasing_stride, output_size=None) -> Tensor:
    return torch.ops.torch_structured.butterfly_multiply_fw(...)

@torch.jit.script
def butterfly_multiply_bw(twiddle, input, grad, increasing_stride) -> Tuple[Tensor, Tensor]:
    return torch.ops.torch_structured.butterfly_multiply_bw(...)

@torch.jit.script
def butterfly_multiply(twiddle, input, increasing_stride, output_size=None) -> Tensor:
    return torch.ops.torch_structured.butterfly_multiply(...)  # ← C++ op, bypasses _ops
```

From `torch_structured/_ops.py:82-107` (per-op probe pattern — clone-and-multiplex):
```python
def _has_cuda_legacy_diag_mult() -> bool:
    try:
        from torch_structured._cuda_legacy.diag_mult import HAS_CUDA_LEGACY_DIAG_MULT
        return HAS_CUDA_LEGACY_DIAG_MULT
    except ImportError:
        return False
```

From `tests/conftest.py:34-42` (current 2-axis fixture):
```python
@pytest.fixture(params=["torch", "triton"])
def backend(request):
    if request.param == "triton" and not torch_structured._ops._has_any_triton_kernel():
        pytest.skip("No Triton kernel installed")
    original = torch_structured._ops._BACKEND
    chosen = torch_structured._ops.set_backend(request.param)
    yield chosen
    torch_structured._ops.set_backend(original)
```

From `python -c "import inspect; ..."` (CAPTURED 2026-05-28 — use these EXACT strings in test_public_api_*):
```
Butterfly.__init__:        (self, in_size, out_size, bias=True, complex=False, increasing_stride=True, init='randn', nblocks=1)
ButterflyBmm.__init__:     (self, in_size, out_size, matrix_batch=1, bias=True, complex=False, increasing_stride=True, init='randn', nblocks=1)
ButterflyUnitary.__init__: (self, in_size, out_size, bias=True, increasing_stride=True, nblocks=1)
ButterflyBase4.__init__:   (self, *args, **kwargs)
```

From `torch_structured/factory.py:102-109` (CORRECTED signature — CONTEXT.md was wrong):
```python
def make_linear(kind: str, in_features: int, out_features: int, *, bias: bool = True, **kwargs) -> nn.Module:
```
Correct call: `make_linear("butterfly", 256, 256)` (positional `kind`; NOT `structure='butterfly'`).

From `torch_structured/recurrent/lru.py:203-219`:
```python
class LRU(nn.Module):
    def __init__(self, input_size, hidden_size, num_layers=1, bias=True, batch_first=False,
                 dropout=0.0, bidirectional=False, *, kind=None, kind_B='dense', kind_C='dense', ...)
```
Correct call: `LRU(input_size=64, hidden_size=64, batch_first=True, kind='butterfly')`.
</interfaces>

<tolerances>
Inherited from Phase 7 D-43a + Phase 8 D-52 (per 09-CONTEXT.md D-62c):
- fp32 forward + d_input: `rtol=1e-5, atol=1e-6`
- complex64 d_input: `rtol=1e-5, atol=1e-6`
- complex64 d_twiddle: `rtol=1e-3, atol=1e-4`
- v1.0/v1.1 checkpoint round-trip forward output across BACKEND=triton vs BACKEND=cuda: `rtol=1e-3, atol=1e-3` (loose because fp32 accumulation across log_n=8 stages — RESEARCH §7).
</tolerances>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the §0 LANDMINE — rewrite butterfly/multiply.py as D-05 attribute-access delegators</name>
  <files>torch_structured/butterfly/multiply.py, tests/test_phase9_integration.py</files>
  <read_first>
    - .planning/phases/09-integration-hardening-correctness-gates/09-CONTEXT.md (entire file — domain + D-60..D-72)
    - .planning/phases/09-integration-hardening-correctness-gates/09-RESEARCH.md §0 "CRITICAL LANDMINE" (lines 108-141) — explains why this is foundational
    - .planning/phases/09-integration-hardening-correctness-gates/09-PATTERNS.md "Pattern 1: D-05 attribute-access delegator" (lines 974-988) — the verbatim template
    - torch_structured/structured/hadamard.py (entire 35-line file — the VERBATIM template to mirror per PATTERNS Pattern 1)
    - torch_structured/butterfly/multiply.py (current 31 lines — the LANDMINE source to rewrite)
    - torch_structured/butterfly/butterfly.py:1-20 (verify the import surface `from .multiply import butterfly_multiply` at line 8 still resolves)
    - tests/test_butterfly_triton.py:43-45 (Phase 8 SC#4 imports `_legacy_mod_for_sc4 = butterfly.multiply` and monkey-patches `butterfly_multiply_fw`/`_bw` — verify the new delegators preserve monkey-patchability by NOT using @torch.jit.script)
  </read_first>
  <behavior>
    - Test 1 (`test_butterfly_nn_module_routes_through_ops`): After `set_backend('triton')`, instantiate `Butterfly(in_size=256, out_size=256, bias=False).cuda()`, monkey-patch `torch_structured.butterfly.multiply.butterfly_multiply` to raise `AssertionError("LANDMINE: nn.Module bypassed _ops")`, call `m(x)`, assert NO raise (because `Butterfly.forward` now calls the delegator which delegates to `_ops`, and `_ops.butterfly_multiply` was bound to the Triton kernel by `set_backend('triton')`). Restore in finally.
    - Test 2 (`test_butterfly_multiply_delegator_is_not_jit_scripted`): `assert not hasattr(torch_structured.butterfly.multiply.butterfly_multiply, 'graph')` — `@torch.jit.script` produces a ScriptFunction with a `.graph` attribute; plain Python function does not. Required for Phase 8 SC#4's monkey-patch (`tests/test_butterfly_triton.py:756-757`) to keep working.
    - Test 3 (`test_butterfly_multiply_fw_bw_still_call_cpp`): Direct module-attribute access `torch_structured.butterfly.multiply.butterfly_multiply_fw(twiddle, x, True, n)` still routes to the C++ op (the `_fw`/`_bw` entry points have no Triton equivalent — they remain C++ direct callers, only `@torch.jit.script` is removed for monkey-patchability per PATTERNS §"Note on @torch.jit.script removal").
  </behavior>
  <action>
Rewrite `torch_structured/butterfly/multiply.py` ENTIRELY per the §0 LANDMINE fix (RESEARCH Option A — single-point delegator). Replace the three `@torch.jit.script`-decorated wrappers with the D-05 attribute-access pattern verbatim from `torch_structured/structured/hadamard.py:25-34`. Per D-67 (inherits Phase 4 D-05) and PATTERNS Pattern 1.

Specific edits:

1. Module docstring at top (NEW — explains the LANDMINE fix; cites §0 + the hadamard.py analog).

2. Imports: keep `import math`, `from typing import Tuple, Optional`, `import torch`, `from torch.nn import functional as F`. Add `import torch_structured  # noqa: F401 — attribute-access target for the delegator below`. Keep the existing `from torch_structured._torch_ref.butterfly import butterfly_multiply_torch  # noqa: F401` line.

3. Replace `butterfly_multiply_fw` (lines 13-17): remove the `@torch.jit.script` decorator (required so Phase 8 SC#4's monkey-patch at `tests/test_butterfly_triton.py:756-757` keeps working per RESEARCH §0 Note + Pitfall 6 + 08-CONTEXT D-53). Keep the body — direct call to `torch.ops.torch_structured.butterfly_multiply_fw(...)` — and the type hints. Add a one-line docstring referencing the SC#4 monkey-patch contract. NOTE: no Triton equivalent exists for the `_fw`-only entry point (the Triton path is autograd-aware via `butterfly_multiply`); this stays on the C++ op.

4. Replace `butterfly_multiply_bw` (lines 20-23): same treatment — remove `@torch.jit.script`, keep the C++ call body.

5. Replace `butterfly_multiply` (lines 26-30): rewrite as a D-05 delegator. NO `@torch.jit.script`. NO direct `torch.ops.torch_structured.butterfly_multiply(...)` call. Body: `return torch_structured._ops.butterfly_multiply(*args, **kwargs)`. Signature accepts `*args, **kwargs` (matches the `structured/hadamard.py:25` analog — simpler than re-declaring the 4 params and let `_ops.butterfly_multiply` validate). Docstring cites §0 + D-05 + the historical import surface preserved.

Add 3 tests to `tests/test_phase9_integration.py` (new file — Task 1 creates the file; Task 2 extends it). Per PATTERNS §"tests/test_phase9_integration.py" imports section, use module-level `pytestmark = pytest.mark.skipif(not torch.cuda.is_available(), reason=...)`. Reference markers: `@pytest.mark.op('butterfly_multiply')` on Test 1 (so the future cuda axis from Task 2 can skip-gate properly).

For Test 1 use the Phase 8 SC#4 monkey-patch pattern verbatim — store original, replace with raising stub, call forward, assert stub not invoked (no raise), restore in `finally`. The monkey-patch target is `torch_structured.butterfly.multiply.butterfly_multiply` (the delegator). After the fix, `Butterfly.forward` imports `butterfly_multiply` from `.multiply` which is now the delegator — replacing the module attribute changes what `Butterfly.forward` sees on the NEXT call (Python re-reads module attributes through the bound name at call time IF the bound name was an attribute access; but `from X import Y` binds `Y` in the importing namespace at import time). Therefore: in Test 1, ALSO monkey-patch `torch_structured.butterfly.butterfly.butterfly_multiply` (the symbol bound inside `Butterfly.forward`'s closure) — this is the load-bearing one. Document this in a comment: the §0 fix makes the delegator re-read `_ops` on every call, but the `Butterfly.forward` namespace binding to `multiply.butterfly_multiply` is still resolved-at-import. Both monkey-patch targets must be set so the test correctly asserts the delegator path is exercised.

Quality gates after Task 1:
- `python -c "from torch_structured.butterfly.multiply import butterfly_multiply, butterfly_multiply_fw, butterfly_multiply_bw, butterfly_multiply_torch; print('OK')"` prints OK.
- `grep -nE 'torch\.ops\.torch_structured\.butterfly_multiply\b' torch_structured/butterfly/multiply.py | grep -v '^#'` returns ONLY the `_fw` and `_bw` lines (not the plain `butterfly_multiply`). The line `return torch.ops.torch_structured.butterfly_multiply(...)` must NOT exist after the fix.
- `grep -c '@torch.jit.script' torch_structured/butterfly/multiply.py` returns 0 (all three decorators removed for monkey-patchability).
- The Phase 8 SC#4 test (`tests/test_butterfly_triton.py:705-774`) STILL PASSES — verify by running `pytest tests/test_butterfly_triton.py::test_butterfly_backward_no_cpp_symbol -xvs`. If it fails, Plan 09-03 Task 4 will reconcile per D-61a; Plan 09-01's contract is that the test still passes UNDER THE CURRENT routing-table-empty state (no _routing.json yet).
  </action>
  <verify>
    <automated>pytest tests/test_phase9_integration.py::test_butterfly_nn_module_routes_through_ops tests/test_phase9_integration.py::test_butterfly_multiply_delegator_is_not_jit_scripted tests/test_phase9_integration.py::test_butterfly_multiply_fw_bw_still_call_cpp tests/test_butterfly_triton.py::test_butterfly_backward_no_cpp_symbol -xvs</automated>
    <grep>grep -nE 'return torch\.ops\.torch_structured\.butterfly_multiply\(' torch_structured/butterfly/multiply.py | grep -v '_fw\|_bw' | grep -v '^[^:]*:[^:]*:#'</grep>
    <expect>The grep returns NO lines (empty output). Any non-empty output means a `torch.ops.torch_structured.butterfly_multiply(...)` direct call survived — the LANDMINE fix is incomplete.</expect>
    <grep>grep -cE '^@torch\.jit\.script' torch_structured/butterfly/multiply.py</grep>
    <expect>0</expect>
  </verify>
  <done>
    - `torch_structured/butterfly/multiply.py` rewritten with three plain-Python delegators (no `@torch.jit.script`); the `butterfly_multiply` delegator calls `torch_structured._ops.butterfly_multiply(*args, **kwargs)` via attribute access.
    - 3 LANDMINE tests in `tests/test_phase9_integration.py` pass.
    - Phase 8 SC#4 test `test_butterfly_backward_no_cpp_symbol` still passes (it monkey-patches `_fw`/`_bw` on the module — the `@torch.jit.script` removal preserves monkey-patchability per Pitfall 6).
    - Grep gate hygiene: no `return torch.ops.torch_structured.butterfly_multiply(...)` direct-call line survives.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen tests/conftest.py to 3-axis backend fixture + add _has_cuda_legacy_for_op probe</name>
  <files>torch_structured/_ops.py, tests/conftest.py, tests/test_phase9_integration.py</files>
  <read_first>
    - .planning/phases/09-integration-hardening-correctness-gates/09-CONTEXT.md §"CUDA conftest skip-gate" (D-62, D-62a, D-62b, D-62c)
    - .planning/phases/09-integration-hardening-correctness-gates/09-RESEARCH.md §"Phase requirements" TEST-03 row + §"User Constraints" D-62 group
    - .planning/phases/09-integration-hardening-correctness-gates/09-PATTERNS.md §"torch_structured/_ops.py" 09-01 addition (lines 109-154) + §"tests/conftest.py" widening (lines 326-395)
    - torch_structured/_ops.py:82-154 (existing `_has_cuda_legacy_diag_mult`, `_has_cuda_legacy_hadamard`, `_has_triton_kernel`, `_has_any_triton_kernel` — the analog probe surface)
    - tests/conftest.py (entire 43-line file — current 2-axis Phase 6 D-39 implementation)
    - .planning/phases/06-hadamard-triton-port/06-CONTEXT.md D-39 (the precedent for the skip-gate widening pattern)
  </read_first>
  <behavior>
    - Test 1 (`test_has_cuda_legacy_for_op_butterfly_multiply`): `_has_cuda_legacy_for_op('butterfly_multiply')` returns the same bool as `_has_cuda_legacy()`. Never raises.
    - Test 2 (`test_has_cuda_legacy_for_op_diag_mult`): `_has_cuda_legacy_for_op('diag_mult')` returns the same bool as `_has_cuda_legacy_diag_mult()`.
    - Test 3 (`test_has_cuda_legacy_for_op_hadamard_transform`): `_has_cuda_legacy_for_op('hadamard_transform')` returns the same bool as `_has_cuda_legacy_hadamard()`.
    - Test 4 (`test_has_cuda_legacy_for_op_unknown_returns_false`): `_has_cuda_legacy_for_op('nonexistent_op')` returns `False` (NO try/except inside the function — D-21 already covers the per-op probes; the unknown-op case takes the explicit `return False` branch).
    - Test 5 (`test_backend_fixture_skips_cuda_axis_when_marker_present_and_so_missing`): a tiny synthetic test bearing `@pytest.mark.op('nonexistent_op')` and using `backend` SKIPS the cuda parametrize value (the test passes for torch + triton, skips for cuda).
    - Test 6 (`test_backend_fixture_does_not_skip_cuda_axis_without_marker`): a synthetic test using `backend` WITHOUT any `@pytest.mark.op` marker DOES NOT skip cuda (marker-less tests are backend-agnostic per PATTERNS comment).
  </behavior>
  <action>
**Part A — `torch_structured/_ops.py` adds `_has_cuda_legacy_for_op(op_name)`** per D-62a + PATTERNS lines 132-154.

Insert AFTER `_has_cuda_legacy_hadamard` (after line 107) and BEFORE the `_TRITON_PACKAGE_NAMES` dict at line 114. Body multiplexes the three existing per-op probes by `op_name`:
- `"butterfly_multiply"` → `_has_cuda_legacy()` (the eagerly-loaded `_butterfly.so` symbol — verified at `_ops.py:72-79`)
- `"diag_mult"` → `_has_cuda_legacy_diag_mult()`
- `"hadamard_transform"` → `_has_cuda_legacy_hadamard()`
- anything else → `False` (explicit branch, no try/except per CLAUDE.md no-try/except-in-core rule — the existing probes own all the try/except surface)

Symmetric to `_has_triton_kernel(op_name)` at lines 119-139 (PATTERNS Pattern 2). Docstring cites D-62a + the per-op skip-gate consumer (`tests/conftest.py`).

**Part B — `tests/conftest.py` widens to 3-axis** per D-62 + PATTERNS lines 326-395.

Update `pytest_configure(config)` (lines 22-31) to register THREE additional markers:
- existing `slow` — unchanged
- NEW `multigpu` — "requires >=2 GPUs (Phase 9 D-64 — opt-in via torchrun --nproc_per_node=2)"  ← reserved for Plan 09-02 FSDP test; registered here so 09-01 ships a complete marker set
- NEW `op(name)` — "bind a test to a specific op for per-op cuda skip-gate (Phase 9 D-62b; e.g., @pytest.mark.op('butterfly_multiply'))"

Update the `backend` fixture (lines 34-42) to `params=["torch", "triton", "cuda"]` with the cuda axis skip-gate. Marker-based op-name detection per D-62b option (1) — recommended in CONTEXT + RESEARCH. Per PATTERNS lines 371-394:
- Existing triton skip-gate via `_has_any_triton_kernel()` — keep unchanged.
- NEW cuda skip-gate: `op_marker = request.node.get_closest_marker("op")`. If marker exists AND its first arg is the op_name AND `not _has_cuda_legacy_for_op(op_name)`, `pytest.skip(f"No CUDA legacy .so for {op_name}")`. If marker absent, do NOT skip cuda (treat as backend-agnostic).
- Existing save/restore via `set_backend(original)` in teardown — keep unchanged (D-22 + PATTERNS Pattern 3).

Module docstring at top: update to reflect 3-axis (the existing docstring at lines 1-16 says "Phase 7+ will extend params to [torch, triton, cuda]" — replace with "Phase 9 D-62 widens to 3-axis with per-op skip-gate via @pytest.mark.op('<op_name>') markers (D-62b option 1).").

**Part C — Tests in `tests/test_phase9_integration.py`** (extends the file created in Task 1).

Add 4 unit tests for `_has_cuda_legacy_for_op` (cover all four branches: butterfly_multiply, diag_mult, hadamard_transform, unknown). Add 2 fixture-behavior tests using nested-marker inline params (the marker-based skip mechanism doesn't allow easy inline assertion — workaround: use `pytest.param('triton', marks=[pytest.mark.op('nonexistent_op')])` to verify the conftest cuda axis skips correctly when the marker names a non-existent op).

Quality gates:
- `grep -c "_has_cuda_legacy_for_op" torch_structured/_ops.py` returns 2 (definition + docstring reference) or more; was 0 before.
- `grep -c 'params=\["torch", "triton", "cuda"\]' tests/conftest.py` returns 1; was 0 before.
- `pytest tests/test_phase9_integration.py::test_has_cuda_legacy_for_op_butterfly_multiply tests/test_phase9_integration.py::test_has_cuda_legacy_for_op_diag_mult tests/test_phase9_integration.py::test_has_cuda_legacy_for_op_hadamard_transform tests/test_phase9_integration.py::test_has_cuda_legacy_for_op_unknown_returns_false -xvs` all pass.
- Sanity: running ANY existing test from `tests/test_butterfly_triton.py` (e.g., `pytest tests/test_butterfly_triton.py::test_butterfly_eager_fp32 -xvs`) STILL works under the widened conftest (the cuda axis silently skips when no `@pytest.mark.op` marker is present, per the "do NOT skip without marker" rule).
  </action>
  <verify>
    <automated>pytest tests/test_phase9_integration.py::test_has_cuda_legacy_for_op_butterfly_multiply tests/test_phase9_integration.py::test_has_cuda_legacy_for_op_diag_mult tests/test_phase9_integration.py::test_has_cuda_legacy_for_op_hadamard_transform tests/test_phase9_integration.py::test_has_cuda_legacy_for_op_unknown_returns_false -xvs</automated>
    <grep>grep -nE '_has_cuda_legacy_for_op' torch_structured/_ops.py | grep -v '^[^:]*:[^:]*:#' | head -10</grep>
    <expect>At least 2 lines: the def line AND at least one reference (docstring or another internal use).</expect>
    <grep>grep -c 'params=\["torch", "triton", "cuda"\]' tests/conftest.py</grep>
    <expect>1</expect>
  </verify>
  <done>
    - `_has_cuda_legacy_for_op` defined in `_ops.py`, multiplexes the existing per-op probes, never raises, returns False for unknown op_names.
    - `tests/conftest.py` `backend` fixture has 3 params; the `cuda` param per-test-skips via `@pytest.mark.op('<op_name>')` marker per D-62b.
    - `multigpu` and `op` markers registered in `pytest_configure` (no PytestUnknownMarkWarning).
    - 4 probe-unit tests pass; sanity-run of an existing Phase 7/8 test still passes (no regression from the widening).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Backend-agreement + checkpoint round-trip + make_linear/LRU + public API tests in tests/test_phase9_integration.py</name>
  <files>tests/test_phase9_integration.py</files>
  <read_first>
    - .planning/phases/09-integration-hardening-correctness-gates/09-CONTEXT.md §"In scope" 09-01 bullets (lines 15-24)
    - .planning/phases/09-integration-hardening-correctness-gates/09-RESEARCH.md §7 "Checkpoint round-trip" (lines 617-686), §10 "make_linear + LRU" (lines 893-971), §13 "Public API regression detector" (lines 1105-1162)
    - .planning/phases/09-integration-hardening-correctness-gates/09-PATTERNS.md §"tests/test_phase9_integration.py" (lines 627-703)
    - torch_structured/_torch_ref/butterfly.py:1-33 (the oracle — used as the reference for backend agreement)
    - torch_structured/butterfly/butterfly.py:30-60 (Butterfly.__init__ — signature already CAPTURED in <interfaces> above)
    - torch_structured/factory.py:102-136 (make_linear — CAPTURED signature in <interfaces>)
    - torch_structured/recurrent/lru.py:189-294 (LRU — CAPTURED signature in <interfaces>)
    - tests/test_butterfly_triton.py:37-72 (imports + first parametrized backend test — analog for the backend-agreement pattern)
  </read_first>
  <behavior>
    - Test 1 (`test_backend_agreement_butterfly_fp32`, `@pytest.mark.op('butterfly_multiply')`, parametrized log_n in {2, 4, 8}): under each backend, `torch_structured._ops.butterfly_multiply(twiddle, input, True, n)` matches `_torch_ref.butterfly.butterfly_multiply_torch(twiddle, input, True, n)` at rtol=1e-5/atol=1e-6. Inputs: `(nstacks=1, nblocks=1)`, twiddle shape `(1, 1, log_n, n//2, 2, 2)`, input shape `(4, 1, n)`, dtype=fp32, device=cuda.
    - Test 2 (`test_backend_agreement_butterfly_complex64`, `@pytest.mark.op('butterfly_multiply')`, parametrized log_n in {2, 4, 8}): same as Test 1 but dtype=complex64; tolerance from Phase 7 D-43a (`rtol=1e-3, atol=1e-4` for complex64 forward — same as the existing test at tests/test_butterfly_triton.py:84-97).
    - Test 3 (`test_backend_agreement_diag_mult`, `@pytest.mark.op('diag_mult')`, parametrized log_n in {4, 8}): under each backend, `torch_structured._ops.diag_mult(...)` matches the oracle at fp32 (rtol=1e-5/atol=1e-6) and complex64 (rtol=1e-4).
    - Test 4 (`test_backend_agreement_hadamard_transform`, `@pytest.mark.op('hadamard_transform')`, parametrized log_n in {4, 8}): under each backend, `torch_structured._ops.hadamard_transform(...)` matches the oracle at fp32 (rtol=1e-5/atol=1e-6).
    - Test 5 (`test_checkpoint_v10_v11_roundtrip_butterfly_fp32`, `@pytest.mark.op('butterfly_multiply')`): synthesize a `Butterfly(256, 256, bias=False, complex=False, increasing_stride=True)` state_dict (the layout matches v1.0/v1.1 unchanged since Phase 1), `torch.save` to `tmp_path / "v10_v11_butterfly.pt"`, `torch.load` into a fresh `Butterfly` under `BACKEND=triton`, compare forward output to the same load under `BACKEND=cuda` within `rtol=1e-3/atol=1e-3` (RESEARCH §7 — fp32 accumulation tolerance). Skip when `_has_cuda_legacy_for_op('butterfly_multiply')` returns False.
    - Test 6 (`test_make_linear_butterfly_triton`, `@pytest.mark.op('butterfly_multiply')`, single fixture pass at backend='triton'): `make_linear("butterfly", 256, 256).cuda()` forward + backward, assert `m.b.twiddle.grad is not None and torch.isfinite(m.b.twiddle.grad).all()`. Use the CORRECTED signature (positional `kind`).
    - Test 7 (`test_lru_butterfly_triton`, `@pytest.mark.op('butterfly_multiply')`): `LRU(input_size=64, hidden_size=64, num_layers=1, batch_first=True, kind='butterfly').cuda()` on `(4, 32, 64)` input, forward+backward, assert ANY parameter has finite gradient (LRU has complex-state scan; the butterfly projections are real fp32 — per RESEARCH §10).
    - Test 8 (`test_public_api_butterfly_init_signature`): `str(inspect.signature(Butterfly.__init__))` matches the CAPTURED string in <interfaces>. Three more tests for `ButterflyBmm`, `ButterflyUnitary`, `ButterflyBase4` (4 tests total).
    - Test 9 (`test_pytest_under_triton_smoke`): SUBPROCESS-runs `subprocess.run([sys.executable, "-m", "pytest", "tests/test_phase9_integration.py::test_backend_agreement_butterfly_fp32", "-x", "--no-header", "-q"], env={**os.environ, "TORCH_STRUCTURED_BACKEND": "triton"}, capture_output=True, text=True, timeout=120)`. Assert `result.returncode == 0`. This is the lightweight TEST-06 in-tree check — full `pytest tests/` under BACKEND=triton is gated at CI level (covered by Plan 09-03 CI matrix entry).
  </behavior>
  <action>
Implement the 9 tests above in `tests/test_phase9_integration.py`. Module-level setup:
- Docstring (per PATTERNS lines 633-645 — Phase 9 09-01 integration tests + tolerances note).
- `pytestmark = pytest.mark.skipif(not torch.cuda.is_available(), reason="Phase 9 integration tests require CUDA")` per PATTERNS Pattern 4.
- Imports: `inspect`, `os`, `subprocess`, `sys`, `pytest`, `torch`, `torch_structured`, `from torch_structured import Butterfly, ButterflyBmm, ButterflyBase4, ButterflyUnitary, LRU, make_linear`, `from torch_structured._torch_ref.butterfly import butterfly_multiply_torch`, `from torch_structured._torch_ref.diag_mult import diag_mult as _diag_mult_torch`, `from torch_structured._torch_ref.hadamard import hadamard_transform_torch`.

Tolerances (single-source, defined as module-level constants per CLAUDE.md `assert` precondition style — fail-loud on dtype mismatch):
- `_FP32_RTOL = 1e-5; _FP32_ATOL = 1e-6` (D-62c, Phase 7+8)
- `_C64_DTWIDDLE_RTOL = 1e-3; _C64_DTWIDDLE_ATOL = 1e-4` (D-62c, Phase 8)
- `_C64_DINPUT_RTOL = 1e-5; _C64_DINPUT_ATOL = 1e-6` (D-62c)
- `_C64_FORWARD_RTOL = 1e-3; _C64_FORWARD_ATOL = 1e-4` (Phase 7 D-43a forward — matches the existing tests/test_butterfly_triton.py:84-97 pattern)
- `_CKPT_FORWARD_RTOL = 1e-3; _CKPT_FORWARD_ATOL = 1e-3` (RESEARCH §7 — fp32 accumulation over log_n=8)

Test bodies — translate behavior block above into concrete tests. For backend-agreement, use the `backend` fixture from conftest. For each test the param `backend` will be one of {torch, triton, cuda} per parametrization; conftest has already set the backend before the test body runs.

The CAPTURED signatures for Test 8 (from <interfaces>) — use them verbatim as the expected strings. Document the Python version assumption (3.10+ per project floor; the signature string format is stable across 3.10-3.12 for these signatures — verified at task time).

For Test 5 (checkpoint round-trip), the test creates TWO `Butterfly(256, 256, ...)` instances (one under `set_backend('triton')`, one under `set_backend('cuda')`), loads the SAME `state_dict` into both, and compares forward outputs on the SAME input. Both instances are constructed AFTER `set_backend(...)` so the resolver has bound to the right backend. Note: the `backend` fixture already manages setBackend save/restore — Test 5 needs explicit set_backend calls inside the test body because it spans TWO backends; use `try/finally` to restore. Use `_has_cuda_legacy_for_op('butterfly_multiply')` (the Task 2 probe) as the skip predicate.

For Test 9 (subprocess pytest under env=triton), use a SMALL test target (single test function) — not full `tests/` — to keep wall time under the 120-second timeout. The CI matrix entry (Plan 09-03) runs the full suite; Test 9 is the in-process smoke that the env-var-driven path resolves correctly.

Quality gates after Task 3:
- File has ~9 test functions + module docstring + module-level pytestmark.
- `pytest tests/test_phase9_integration.py -xvs --no-header` runs all tests and exits 0 (subject to `_has_cuda_legacy_for_op('butterfly_multiply')` skip-gate behaviour — some cuda axes may skip; that's correct per D-62).
- `grep -c '@pytest.mark.op' tests/test_phase9_integration.py` returns ≥ 7 (every parametrized cross-backend test bears a marker — explicit-is-better per D-62b).
  </action>
  <verify>
    <automated>pytest tests/test_phase9_integration.py -xvs --no-header</automated>
    <grep>grep -cE '^@pytest\.mark\.op\(' tests/test_phase9_integration.py</grep>
    <expect>≥ 7 (every parametrized cross-backend test has an explicit op marker per D-62b).</expect>
    <grep>grep -cE 'def test_' tests/test_phase9_integration.py</grep>
    <expect>≥ 13 (3 LANDMINE tests from Task 1 + 6 conftest probe/fixture tests from Task 2 + 4 public-API signature tests + backend-agreement + checkpoint + make_linear + LRU + subprocess-pytest = 13+, exact count depends on the parametrize axes which test framework counts as one def each).</expect>
  </verify>
  <done>
    - 9 new tests (plus the Task 1+2 tests already in this file) exercise the full SC#1+SC#3 surface.
    - Backend-agreement tests cover butterfly_multiply, diag_mult, hadamard_transform at fp32 + complex64 (where applicable) under all 3 backends.
    - Checkpoint round-trip test demonstrates COMPAT-02 — v1.0/v1.1 layout `(nstacks, nblocks, log_n, n/2, 2, 2)` loads via `Butterfly.load_state_dict` under BACKEND=triton and produces forward output identical to BACKEND=cuda within fp32 tolerance.
    - `make_linear` and `LRU` smoke tests demonstrate COMPAT-03.
    - Public API regression detector (inspect.signature snapshot for 4 classes) demonstrates COMPAT-01.
    - Subprocess-pytest smoke test exercises TEST-06.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Python code → torch_structured.set_backend() | User passes a backend name string; `_resolve()` validates against the closed set {triton, cuda, torch, auto} per T-04-01 mitigation; Phase 9 inherits without re-opening. |
| User test code → @pytest.mark.op('<op_name>') | Marker args are untrusted strings; `_has_cuda_legacy_for_op` returns False for unknown op_names (does not raise) so a typo skips the cuda axis safely. |
| Filesystem → tests/test_phase9_integration.py Test 5 (checkpoint round-trip) | The `tmp_path` `torch.save` / `torch.load` write+read is INSIDE the test fixture; pytest manages `tmp_path` cleanup. No persistent state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | T (Tampering) | `_has_cuda_legacy_for_op` unknown op_name | accept | Returns False explicitly for unknown op_names (no raise, no dynamic import); never executes attacker-controlled code. The op_name string is `pytest.mark.op` args — already trusted to come from test source. |
| T-09-02 | I (Information disclosure) | inspect.signature snapshot in test_public_api_* | accept | Signatures are public API surface; no secrets disclosed. The hardcoded reference strings are CAPTURED from the live module at task time, not from training data. |
| T-09-03 | D (Denial of service) | `subprocess.run([..., 'pytest', ...])` in `test_pytest_under_triton_smoke` | mitigate | `timeout=120` argument bounds the subprocess wall time; the test target is a SINGLE test function (not full `tests/`), bounding iteration count. CI-level full-suite run is gated separately (Plan 09-03). |
| T-09-04 | E (Elevation of privilege) | torch.save / torch.load (pickle-based) in checkpoint round-trip test | accept | The `state_dict` is synthesized in-process from `Butterfly(...).state_dict()` — never reads attacker-controlled bytes. The tmp_path round-trip exercises the FORMAT, not deserialization of untrusted input. |
| T-09-05 | S (Spoofing) | `@pytest.mark.op('butterfly_multiply')` marker | accept | The marker is on test source code — already trusted. The cuda skip-gate's contract (skip when .so missing) is fail-safe: misclassified ops fall through to torch path which is the safest backend. |
</threat_model>

<verification>
- The §0 LANDMINE is fixed: `grep -nE 'return torch\.ops\.torch_structured\.butterfly_multiply\(' torch_structured/butterfly/multiply.py | grep -v '_fw\|_bw'` returns empty.
- `pytest tests/test_phase9_integration.py -xvs --no-header` exits 0 (all tests pass or skip cleanly per the per-op cuda skip-gate).
- `pytest tests/test_butterfly_triton.py -k 'not slow' -xvs --no-header` exits 0 (Phase 7+8 regression check — the LANDMINE fix + conftest widening do not break existing tests).
- `pytest tests/test_dispatch.py -xvs --no-header` exits 0 (Phase 4 dispatch contract still holds).
- `pytest tests/structured/ -xvs --no-header` exits 0 (Phase 6 hadamard / Phase 5 diag_mult tests survive the conftest widening — they use the marker-less default which does NOT skip cuda).
</verification>

<success_criteria>
- SC#1 (foundation): `backend` fixture parametrizes 3-axis; cuda axis per-op skipped via `@pytest.mark.op('<op>')`; backend-agreement holds at documented tolerances.
- SC#3 (checkpoint round-trip): v1.0/v1.1 layout state_dict loads via `Butterfly.load_state_dict` under BACKEND=triton; forward output matches BACKEND=cuda within rtol=1e-3/atol=1e-3.
- §0 LANDMINE fix (foundational): `Butterfly(...).forward(x)` under set_backend('triton') invokes the Triton kernel, not the C++ op. Verified by Task 1 Test 1.
- COMPAT-01: 4 public-API signature snapshot tests detect any future regression.
- COMPAT-02: Checkpoint round-trip test demonstrates the twiddle layout is preserved.
- COMPAT-03: make_linear + LRU smoke tests pass under BACKEND=triton.
- TEST-06: in-tree subprocess-pytest smoke test exits 0 under BACKEND=triton (full-suite CI gate is Plan 09-03).
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-hardening-correctness-gates/09-01-SUMMARY.md` documenting:
  - The §0 LANDMINE fix (multiply.py rewrite + verification mechanism)
  - The conftest widening + the marker convention adopted
  - The 9 new tests + which SC they cover + actual tolerance reached vs target
  - Any unexpected interactions discovered (e.g., did removing @torch.jit.script break anything? did the Phase 8 SC#4 test need any adjustment beyond Plan 09-03?)
</output>
