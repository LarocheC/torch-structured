---
phase: 06-hadamard-triton-port
verified: 2026-05-27T17:30:00Z
status: passed
score: 13/13 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 12/13
  previous_verified: 2026-05-27T16:45:00Z
  gaps_closed:
    - "SC#2 (Truth #2 / Truth #13) strict reading — rank-N forward + backward now succeed on both `torch` and `triton` backends per the single-line widening in `torch_structured/_torch_ref/hadamard.py:41` (approach (a) reshape-and-restore)."
  gaps_remaining: []
  regressions: []
deferred:
  - truth: "_cuda_legacy/hadamard.py signature (u) is incompatible with the (u, normalize=False) contract used by the triton/torch bindings (CR-02 from 06-REVIEW.md)."
    addressed_in: "Phase 9 / Phase 10"
    evidence: "Phase 9 success criteria: the backend fixture parametrizes every shared correctness test over {triton, cuda, torch}. Phase 10: cuda backend deprecation. The cuda branch is dormant on this host (_hadamard_cuda.so is not built); SC#1 of Phase 6 explicitly says TORCH_STRUCTURED_BACKEND=triton (not cuda). Phase 9 closure will surface and fix this signature mismatch."
---

# Phase 6: hadamard Triton Port Verification Report

**Phase Goal:** `hadamard` runs on Triton as a forward-only self-inverse kernel, proving the two-pass mixed-radix shared-memory pattern in Triton without atomics or complex
**Verified:** 2026-05-27T17:30:00Z (re-verification after gap closure)
**Status:** passed
**Re-verification:** Yes — after Plan 06-02 gap closure (RED `900838e` + GREEN `960857c`)

## Re-Verification Summary

The prior verification (2026-05-27T16:45:00Z) returned `gaps_found` with 1 gap: SC#2 strict reading (Truth #2 / Truth #13) — rank-3+ inputs failed backward on both backends because `_torch_ref/hadamard.py:41` unpacked `batch_size, n = u.shape`, which is rank-2-only. The Triton backward at `_triton/hadamard_transform/op.py:168` delegates through this oracle, so the bug flowed end-to-end.

Plan 06-02 (gap-closure plan) applied approach (a) reshape-and-restore: capture `original_shape`, extract `n = u.shape[-1]`, reshape to `(-1, n)` for the existing rank-2-correct loop body, and restore the original shape on return. This single-file fix lights up rank-N forward + backward simultaneously on BOTH backends. Three new regression tests (`test_hadamard_eager_fp32_rank3`, `test_hadamard_backward_rank3`, `test_hadamard_self_inverse_rank3`) lock the `(*batch, n)` contract advertised at `_triton/hadamard_transform/op.py:104`.

**Result:** All 13 must-haves now VERIFIED. Both previously-FAIL behavioral spot-check rows (rank-3 backward; rank-3 forward on torch backend) now PASS. Phase 6 closure complete.

## Goal Achievement

### Observable Truths (Plan must_haves)

| #   | Truth                                                                                                                                                                                                       | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                              |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | SC#1: With `TORCH_STRUCTURED_BACKEND=triton`, `_ops.hadamard_transform(u, normalize=False)` matches `_torch_ref.hadamard.hadamard_transform_torch` within rtol=1e-5, atol=1e-6 across log_n ∈ {2..12}        | VERIFIED   | `test_hadamard_eager_fp32[triton-{2..12}]` 11 cases all PASS (re-verified); `test_hadamard_normalize[triton]` PASS. Resolver binding confirmed: `_BACKEND == "triton"` AND `_ops.hadamard_transform is _triton.hadamard_transform.op.hadamard_transform`.                                                                                                              |
| 2   | SC#2: Composing `hadamard_transform ∘ hadamard_transform` yields N·u (unnormalized) or u (normalized) within fp32 noise on any (batch, 2**log_n) fp32 input — fires on any kernel sign error                | VERIFIED   | **UPGRADED FROM PARTIAL.** Rank-2: `test_hadamard_self_inverse[{torch,triton}-{8,10}]` 4 PASS. Rank-3: `test_hadamard_self_inverse_rank3[{torch,triton}]` 2 PASS (NEW, both unnormalized N*u and normalized u variants). Direct rank-3 backward repro: `torch_structured._ops.hadamard_transform(torch.randn(3,2,16, requires_grad=True)).sum().backward()` → `grad.shape == (3,2,16)` on both backends. Rank-4 spot-check also passes (verifier ad-hoc check). |
| 3   | SC#3: `structured/fastfood.fastfood_multiply` routes through `_ops.hadamard_transform` via D-05 attribute access and produces identical outputs on Triton vs torch within rtol=1e-5                          | VERIFIED   | `test_hadamard_module_consumer[{torch,triton}]` 2 PASS (re-verified); `TORCH_STRUCTURED_BACKEND=triton pytest tests/structured/`: 43 PASS, 2 SKIP, 0 FAIL (up from prior 37 PASS + 2 SKIP — the 6 new rank-3 PASS rows account for the delta).                                                                                                                          |
| 4   | D-32 self-inverse register_autograd: backward routes grad_out through `_torch_ref.hadamard.hadamard_transform_torch`; propagates the normalize flag via setup_context; passes torch.autograd.gradcheck at fp64 | VERIFIED   | `test_hadamard_gradcheck_fp64[torch]` PASS; `test_hadamard_gradcheck_fp64[triton]` SKIP (intentional — D-31 fp32-only). _setup_context captures normalize at op.py:155-156; _backward uses ctx.normalize at op.py:168. The widened oracle still satisfies the gradcheck assertion shape contract.                                                                       |
| 5   | D-32c register_fake: `_hadamard_transform_fake(u, normalize=False)` returns torch.empty_like(u); FakeTensorMode traces without "data is not allocated yet" error                                                | VERIFIED   | op.py:175-190 still contains `@hadamard_transform.register_fake` with default `normalize=False` (untouched by gap closure). Tested implicitly through register_autograd plumbing in test_hadamard_gradcheck_fp64.                                                                                                                                                       |
| 6   | D-33/a/b/c: structured/hadamard.py no longer contains `class HadamardTransformCuda`, `hadamard_transform_cuda`, module-level binding, `use_hadamard_transform_cuda` try-import, scipy.linalg, or device      | VERIFIED   | structured/hadamard.py unchanged by gap closure (35 lines); grep counts: 0 hits for any of these names.                                                                                                                                                                                                                                                                |
| 7   | D-33d back-compat shim: structured/hadamard.py re-exports `hadamard_transform_torch` AND a callable `hadamard_transform` (via D-05 attribute access on `torch_structured._ops`)                              | VERIFIED   | `test_imports.py::test_import_structured` PASS — both are imported and `callable()`. Shim untouched by gap closure.                                                                                                                                                                                                                                                    |
| 8   | D-34 consumer refactor (D-05): structured/fastfood.py line 1 = `import torch_structured`; lines 8 + 10 = `torch_structured._ops.hadamard_transform(...)`                                                       | VERIFIED   | fastfood.py untouched by gap closure; grep counts unchanged.                                                                                                                                                                                                                                                                                                            |
| 9   | D-36/a/c per-op resolver wiring: `_has_cuda_legacy_hadamard()` exists; three-branch hadamard binding in `_resolve()` Step 2; per-op log.info extended; stale comment removed                                  | VERIFIED   | _ops.py untouched by gap closure. Captured INFO log at runtime: `per-op bindings: butterfly_multiply=triton, diag_mult=triton, hadamard_transform=triton`.                                                                                                                                                                                                              |
| 10  | D-39 conftest widening: backend skip-gate uses `_has_any_triton_kernel()` (not `_has_triton_kernel('diag_mult')`)                                                                                              | VERIFIED   | tests/conftest.py untouched by gap closure; line 25 still uses `_has_any_triton_kernel()`.                                                                                                                                                                                                                                                                              |
| 11  | D-37 test surface: tests/structured/test_hadamard_triton.py with 5 tests parametrized via the backend fixture using D-05 attribute access                                                                     | VERIFIED   | File now has 8 test functions (was 5; 3 new rank-3 functions appended). Collection: 38 items (was 32; +6 from 3 functions × 2 backend params). 37 PASS + 1 intentional SKIP. All test bodies use `torch_structured._ops.hadamard_transform(...)` (verified by grep — 8+ attribute-access call sites).                                                                   |
| 12  | D-38 no edits to existing test_hadamard.py: the three existing tests pass unchanged via the D-33d shim                                                                                                          | VERIFIED   | `tests/structured/test_hadamard.py`: 4 PASS + 1 SKIP unchanged; tests/structured/test_imports.py: 2 PASS unchanged.                                                                                                                                                                                                                                                     |
| 13  | (Plan SC#2 truth restated above) Self-inverse on any input shape                                                                                                                                              | VERIFIED   | **UPGRADED FROM PARTIAL.** Same evidence as Truth #2. The strict reading "any input shape" is now honored: rank-3 self-inverse PASSES on both backends; the `(*batch, n)` contract in the Triton wrapper docstring at op.py:104 is truthful end-to-end.                                                                                                                |

**Score:** 13/13 truths verified (was 12/13 PARTIAL — gap closed)

### Deferred Items

Items not yet met but explicitly addressed in later milestone phases.

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | `_cuda_legacy/hadamard.py` signature mismatch with `(u, normalize=False)` contract (CR-02 from 06-REVIEW.md) | Phase 9 / Phase 10 | Phase 9 SC#1: "the `backend` fixture parametrizes every shared correctness test over `{triton, cuda, torch}`"; Phase 10 SC#1: cuda backend deprecation. The cuda branch is dormant on this host (.so not built); SC#1 of Phase 6 explicitly says `TORCH_STRUCTURED_BACKEND=triton` (not cuda). |

### Required Artifacts

| Artifact                                                       | Expected                                                                                                                                                                                       | Status   | Details                                                                                                                                                                                                            |
| -------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `torch_structured/_torch_ref/hadamard.py`                      | Pure-PyTorch oracle `hadamard_transform_torch(u, normalize=False)`; gradcheck reference; D-32 backward oracle; rank-N-correct after gap closure                                                | VERIFIED | Now 56 lines (was 48 + 8 from gap-closure comment + reshape/restore lines). `n = u.shape[-1]` at line 47; `u.reshape(-1, n)` at line 48; `out.reshape(original_shape)` at line 56. `batch_size, n = u.shape` is GONE (grep `-v '^#'` → 0). `np.log2` preserved per D-35a (grep → 3 hits).                                                                              |
| `torch_structured/_triton/hadamard_transform/__init__.py`      | Package marker re-exporting hadamard_transform from op.py                                                                                                                                      | VERIFIED | 4 lines; unchanged by gap closure                                                                                                                                                                                   |
| `torch_structured/_triton/hadamard_transform/op.py`            | @triton.jit single-pass shared-memory Walsh-Hadamard kernel + @triton_op wrapper + register_autograd backward (self-inverse) + register_fake                                                   | VERIFIED | 191 lines; UNCHANGED by gap closure (verified by `git diff`). All 5 components present. The forward path at op.py:127 was already rank-N-correct via `n_batch = u.numel() // n`; the backward at op.py:168 delegates through `_torch_ref` so the single-file fix in `_torch_ref/hadamard.py` lights up the Triton backward without touching this file. |
| `torch_structured/_cuda_legacy/hadamard.py`                    | Try-import passthrough to `_hadamard_cuda.hadamard_transform`; HAS_CUDA_LEGACY_HADAMARD sentinel                                                                                               | VERIFIED, with deferred contract concern | UNCHANGED by gap closure (verified by `git diff`). CR-02 stays DEFERRED to Phase 9.                                                                                                                                  |
| `torch_structured/_ops.py`                                     | `_has_cuda_legacy_hadamard()` honest probe; three-branch hadamard_transform binding; per-op log.info extended                                                                                  | VERIFIED | UNCHANGED by gap closure (verified by `git diff`).                                                                                                                                                                    |
| `torch_structured/structured/hadamard.py`                      | Back-compat shim re-exporting `hadamard_transform_torch` + callable `hadamard_transform` (D-05 attribute access)                                                                               | VERIFIED | UNCHANGED by gap closure (verified by `git diff`).                                                                                                                                                                    |
| `torch_structured/structured/fastfood.py`                      | `fastfood_multiply` rewritten per D-05 to use `torch_structured._ops.hadamard_transform(...)` at both call sites                                                                                | VERIFIED | UNCHANGED by gap closure.                                                                                                                                                                                            |
| `tests/conftest.py`                                            | Backend fixture skip-gate widened to `_has_any_triton_kernel()` (D-39)                                                                                                                          | VERIFIED | UNCHANGED by gap closure.                                                                                                                                                                                            |
| `tests/structured/test_hadamard_triton.py`                     | 5 tests covering SC#1/SC#2/SC#3 cross-backend + gradcheck + self-inverse + consumer surface + 3 new rank-3 tests                                                                                | VERIFIED | Now 227 lines (was 140). 8 test functions; 38 collected items; 37 PASS + 1 SKIP. Existing 5 functions byte-identical; 3 new rank-3 functions appended after `test_hadamard_module_consumer`.                          |

### Key Link Verification

| From                                                                                  | To                                                                                                | Via                                                                          | Status   | Details                                                                                                                |
| ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------- |
| `torch_structured/structured/fastfood.py:fastfood_multiply`                           | `torch_structured._ops.hadamard_transform`                                                        | attribute access per D-05 / D-34                                             | WIRED    | grep counts unchanged                                                                                                  |
| `torch_structured/_triton/hadamard_transform/op.py:_backward`                         | `torch_structured._torch_ref.hadamard.hadamard_transform_torch`                                   | imported as `_hadamard_transform_torch`; called in register_autograd backward | WIRED    | Import at op.py:39 verified; call at op.py:168 verified. **Now rank-N-correct** end-to-end: the delegate is widened to accept `(..., n)` and the backward path produces correct rank-N gradients on both backends (re-verified by `test_hadamard_backward_rank3` and direct repro). |
| `torch_structured/_ops.py:_resolve Step 2 (hadamard block)`                            | `_triton.../op.hadamard_transform / _cuda_legacy.hadamard.hadamard_transform / _torch_ref.hadamard_transform_torch` | three-branch per-op binding                                                  | WIRED    | _ops.py untouched; INFO log still reports `hadamard_transform=triton`.                                                  |
| `torch_structured/structured/hadamard.py:back-compat shim`                            | `torch_structured._torch_ref.hadamard.hadamard_transform_torch + torch_structured._ops.hadamard_transform` | re-export shim per D-33d                                                     | WIRED    | structured/hadamard.py untouched.                                                                                       |
| `tests/structured/test_hadamard_triton.py`                                            | `torch_structured._ops.hadamard_transform`                                                        | attribute access through the parametrized backend fixture                    | WIRED    | 8+ attribute-access call sites (3 new rank-3 tests added).                                                              |
| `tests/conftest.py:backend fixture`                                                   | `torch_structured._ops._has_any_triton_kernel`                                                    | widened skip-gate per D-39                                                   | WIRED    | tests/conftest.py untouched.                                                                                            |

### Data-Flow Trace (Level 4)

| Artifact                                          | Data Variable | Source                                                                          | Produces Real Data | Status   |
| ------------------------------------------------- | ------------- | ------------------------------------------------------------------------------- | ------------------ | -------- |
| `_ops.hadamard_transform` (bound to Triton)       | `out`         | `wrap_triton(_hadamard_kernel)[grid](u, out, ...)` writes to a real CUDA buffer; flatten via `n_batch = u.numel() // n` supports rank-N inputs | Yes                | FLOWING  |
| `_triton/.../op._hadamard_kernel`                  | `out_ptr`     | tl.load/tl.store on u_ptr/out_ptr — real device memory operations               | Yes                | FLOWING  |
| `_ops.hadamard_transform` (bound to torch_ref)    | `x`           | reshape `u` to `(-1, n)` → `torch.cat((x[..., ::2, :] + ...))` → reshape back to `original_shape`  | Yes                | FLOWING  |
| `_torch_ref.hadamard.hadamard_transform_torch` (rank-N path) | `out` | reshape-and-restore: `original_shape = u.shape`, work in `(-1, n)`, restore at return; rank-N-correct for any input rank `>= 1` | Yes | FLOWING |
| `structured/fastfood.py:fastfood_multiply`        | `HBx`, `HGPHBx` | `torch_structured._ops.hadamard_transform(...)` — re-reads binding each call    | Yes                | FLOWING  |

### Behavioral Spot-Checks

| Behavior                                                              | Command                                                                                                                                                                                                                              | Result                                                                                | Status |
| --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------- | ------ |
| Env-var triton path resolves correctly                                 | `set_backend('triton'); assert _ops.hadamard_transform is triton kernel`                                                                                                                                                              | True                                                                                  | PASS   |
| Per-op log.info reports all three op bindings                          | `logging.basicConfig(INFO); set_backend('triton')` → captured INFO log                                                                                                                                                              | `per-op bindings: butterfly_multiply=triton, diag_mult=triton, hadamard_transform=triton` | PASS   |
| `_has_cuda_legacy_hadamard()` returns clean bool                        | `_has_cuda_legacy_hadamard()`                                                                                                                                                                                                       | `False` (bool) on this workstation (no `_hadamard_cuda.so`)                            | PASS   |
| `_has_triton_kernel('hadamard_transform')` returns True                | `_has_triton_kernel('hadamard_transform')`                                                                                                                                                                                          | `True`                                                                                | PASS   |
| Rank-3 backward succeeds (triton)                                     | `u = torch.randn(2,3,8,device='cuda',requires_grad=True); torch_structured._ops.hadamard_transform(u).sum().backward(); u.grad.shape`                                                                                                | `torch.Size([2, 3, 8])`                                                              | **PASS** (was FAIL) |
| Rank-3 forward on torch backend                                        | `set_backend('torch'); torch_structured._ops.hadamard_transform(torch.randn(2,3,8))`                                                                                                                                                  | `torch.Size([2, 3, 8])`                                                              | **PASS** (was FAIL) |
| Rank-3 backward succeeds (torch backend, via PyTorch autograd through pure-Python oracle) | `set_backend('torch'); u = torch.randn(3,2,16,device='cuda',requires_grad=True); _ops.hadamard_transform(u).sum().backward()`                                                                       | `grad.shape == (3, 2, 16)`                                                            | PASS (new check) |
| Rank-4 input forward+backward (triton)                                 | `u = torch.randn(2,3,4,16,device='cuda',requires_grad=True); _ops.hadamard_transform(u).sum().backward()`                                                                                                                          | `grad.shape == (2, 3, 4, 16)`                                                        | PASS (new check) |
| Rank-1 input forward (torch backend)                                   | `set_backend('torch'); _ops.hadamard_transform(torch.randn(8))`                                                                                                                                                                      | `torch.Size([8])`                                                                    | PASS (new check) |

### Probe Execution

| Probe | Command                                                                                                                                                                                                                                                                                                                                                                                                              | Result                          | Status |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- | ------ |
| `pytest tests/structured/test_hadamard_triton.py -v` | Phase 6 dedicated test suite (now extended with 3 rank-3 tests)                                                                                                                                                                                                                                                                                                                                | 37 PASS, 1 SKIP                  | PASS   |
| `pytest tests/structured/test_hadamard.py tests/structured/test_imports.py -v` | Back-compat shim regression                                                                                                                                                                                                                                                                                                                                                                                          | 6 PASS, 1 SKIP                   | PASS   |
| `TORCH_STRUCTURED_BACKEND=triton pytest tests/structured/ -v` | SC#3 literal contract (`pass pytest tests/structured/ on the Triton backend`)                                                                                                                                                                                                                                                                                                                                                                | 43 PASS, 2 SKIP, 0 FAIL          | PASS (improved from 37 PASS in prior verification — delta = 6 new rank-3 PASS rows) |
| `pytest tests/test_dispatch.py tests/test_diag_mult.py -v` | Phase 5 regression                                                                                                                                                                                                                                                                                                                                                                                                  | 29 PASS                          | PASS (unchanged) |

### Requirements Coverage

| Requirement | Source Plan | Description                                                                                  | Status                      | Evidence                                                                                                                                                                                                                       |
| ----------- | ----------- | -------------------------------------------------------------------------------------------- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TRI-02      | 06-01-PLAN, 06-02-PLAN | `hadamard` runs on Triton (self-inverse, forward kernel only, fp32)                          | SATISFIED                    | Triton kernel exists, runs correctly fp32 across log_n {2..12}, self-inverse holds for rank-2 AND rank-3 inputs on both backends. The `(*batch, n)` contract in the wrapper docstring is now truthful end-to-end. Originally PARTIALLY SATISFIED in initial verification; upgraded to SATISFIED post gap closure. |

REQUIREMENTS.md maps TRI-02 to Phase 6. No orphaned requirements (Phase 6 only owns TRI-02).

### Anti-Patterns Found

| File                                          | Line | Pattern                                                          | Severity | Impact                                                                                                          |
| --------------------------------------------- | ---- | ---------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------- |
| `torch_structured/_cuda_legacy/hadamard.py`   | 33   | Signature `(u)` lacks `normalize` — incompatible with siblings   | WARNING  | Dormant (.so absent on this host). Would raise TypeError on cuda hosts with `_hadamard_cuda.so` + normalize=True keyword. DEFERRED to Phase 9. |

The prior verification's WARNING on `_torch_ref/hadamard.py:41` (`batch_size, n = u.shape` rank-2-only) is RESOLVED — replaced by the rank-N approach (a) block. No new anti-patterns introduced by gap closure. No TODO/FIXME/HACK/XXX markers in Phase 6-modified files. No empty implementations / placeholders. No hardcoded empty data flowing to rendering.

### Code Review Findings Cross-Reference

The 06-REVIEW.md report identified 2 CRITICAL findings. Updated cross-reference:

- **CR-01 (`_torch_ref/hadamard.py:41` rank-2-only)** → **CLOSED**. The line `batch_size, n = u.shape` is gone; replaced by `original_shape = u.shape; n = u.shape[-1]; u = u.reshape(-1, n); batch_size = u.shape[0]` and a `return out.reshape(original_shape)` on the way out. Approach (a) preserves the verbatim-relocation lineage from `structured/hadamard.py:15-30` (the `np.log2` and `torch.cat` math are untouched per D-35a). The Triton wrapper docstring at `_triton/hadamard_transform/op.py:104` advertising `(*batch, n)` is now truthful end-to-end on both backends.
- **CR-02 (`_ops.py:254-257` cuda branch signature mismatch)** → **DEFERRED** (unchanged). The branch is unreachable on this host (no `.so` built). Phase 6 success criteria explicitly mention `TORCH_STRUCTURED_BACKEND=triton`, not cuda. Phase 9 will exercise the cuda axis when the `backend` fixture is extended; tracked there.

The 4 WARNING and 3 INFO findings from 06-REVIEW.md are not blocking gaps; they are noted here for completeness:
- WR-01 — Wrapper docstring promises `(*batch, n)` — **NOW TRUTHFUL** end-to-end with the gap closure (no docstring edit required because approach (a) makes the contract real).
- WR-02 — No log.warning for triton-requested-but-kernel-missing — minor observability nit.
- WR-03 — No log_n=1 or log_n=0 edge cases — outside SC#1 ({2..12}). Verifier ad-hoc spot-check confirmed rank-1 input `torch.randn(8)` works.
- WR-04 — `_torch_ref/diag_mult` naming inconsistency — Phase 5 artifact; not Phase 6.
- IN-01..03 — minor style / docstring issues — not blocking.

### Human Verification Required

None — all SC contract assessments are programmatically verifiable and passed. SC#1 (rtol=1e-5, atol=1e-6 across log_n {2..12}) verified by `test_hadamard_eager_fp32`. SC#2 (self-inverse "on any input shape") verified by `test_hadamard_self_inverse` (rank-2) + `test_hadamard_self_inverse_rank3` (rank-3) + ad-hoc rank-4 spot-check. SC#3 (`pytest tests/structured/` on triton backend) verified by `TORCH_STRUCTURED_BACKEND=triton pytest tests/structured/` → 43 PASS + 2 SKIP + 0 FAIL.

### Gap Closure Status

| Item | Initial Verification (T16:45:00Z) | Re-Verification (T17:30:00Z) |
|------|-----------------------------------|------------------------------|
| Truth #2 (SC#2 self-inverse on any input shape) | PARTIAL — rank-2 only; rank-3+ FAILs backward on both backends | **VERIFIED** — rank-N forward + backward on both backends |
| Truth #13 (Plan SC#2 restated) | PARTIAL — same as Truth #2 | **VERIFIED** |
| CR-01 (`_torch_ref/hadamard.py:41` rank-2-only) | OPEN — blocker under strict reading | **CLOSED** — line widened, reshape-and-restore applied |
| CR-02 (`_cuda_legacy/hadamard.py` signature mismatch with `normalize`) | DEFERRED | DEFERRED (Phase 9 / Phase 10) |
| Wrapper docstring `(*batch, n)` at `_triton/.../op.py:104` | Load-bearing but misleading | **TRUTHFUL** end-to-end with NO change to wrapper file |
| Behavioral spot-check: "Rank-3 backward succeeds" | FAIL (`ValueError: too many values to unpack`) | **PASS** |
| Behavioral spot-check: "Rank-3 forward on torch backend" | FAIL (same ValueError) | **PASS** |
| Probe: `TORCH_STRUCTURED_BACKEND=triton pytest tests/structured/` | 37 PASS, 2 SKIP, 0 FAIL | **43 PASS, 2 SKIP, 0 FAIL** (improved) |
| TRI-02 requirement | PARTIALLY SATISFIED | **SATISFIED** |
| Overall status | gaps_found | **passed** |
| Score | 12/13 | **13/13** |

---

_Initial verification: 2026-05-27T16:45:00Z (gaps_found, 12/13)_
_Re-verification: 2026-05-27T17:30:00Z (passed, 13/13)_
_Verifier: Claude (gsd-verifier)_
