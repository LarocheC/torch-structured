---
phase: 04-triton-dispatch-infrastructure-foundational-decisions
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - torch_structured/_ops.py
  - torch_structured/_triton/__init__.py
  - tests/conftest.py
  - tests/test_dispatch.py
  - .github/workflows/test.yml
autonomous: true
requirements:
  - TRI-05
  - TEST-05
tags:
  - triton-op
  - register-fake
  - torch-compile
  - dynamo
  - gradcheck
  - ci-cache
  - pytest-fixtures
must_haves:
  truths:
    - "_demo_identity_op is registered via @torch.library.triton_op + wrap_triton + register_autograd + register_fake (TRI-05, D-12, D-13)"
    - "tests/test_dispatch.py contains the eager-fp32, eager-complex64, gradcheck, torch.compile(fullgraph=True), and explicit FakeTensorMode-trace tests — all five must pass (D-14 a/b/c)"
    - "The 260419-p27 dynamo bug ('The tensor has a non-zero number of elements, but its data is not allocated yet') is NOT raised when _demo_identity_op is invoked under FakeTensorMode (the literal acceptance gate — see <verification_pattern> in the planning context)"
    - "tests/conftest.py provides a `backend` fixture parametrized on ['torch'] only in Phase 4; Phase 5+ extends to ['torch','triton','cuda']"
    - "torch_structured/_triton/__init__.py exists as an empty placeholder package (HAS_TRITON probe lives here per RESEARCH §Recommended Project Structure)"
    - ".github/workflows/test.yml exists with actions/cache@v4 keyed on (runner.os, python-version, torch-version, hashFiles('torch_structured/_triton/**/*.py')) — NOT keyed on github.sha (Pitfall 6) — caching path ~/.triton/cache (TEST-05, D-16)"
  artifacts:
    - path: "torch_structured/_ops.py"
      provides: "Plan 04-01 module extended with the _demo_identity_op skeleton (Plan 04-01 created the resolver; Plan 04-02 adds the demonstrator below the resolver block)"
      contains: "_demo_identity_op"
    - path: "torch_structured/_triton/__init__.py"
      provides: "Empty placeholder package; HAS_TRITON probe; Phase 5+ kernel home"
      min_lines: 3
    - path: "tests/conftest.py"
      provides: "pytest backend fixture parametrized on ['torch']"
      contains: "def backend("
    - path: "tests/test_dispatch.py"
      provides: "Five demonstrator tests including the 260419-p27 FakeTensorMode reproducer"
      contains: "test_demo_identity_compile_fake_tensor_trace"
    - path: ".github/workflows/test.yml"
      provides: "CI workflow with actions/cache@v4 for ~/.triton/cache"
      contains: "actions/cache@v4"
  key_links:
    - from: "tests/test_dispatch.py"
      to: "torch_structured/_ops.py::_demo_identity_op"
      via: "from torch_structured._ops import _demo_identity_op"
      pattern: "from torch_structured._ops import _demo_identity_op"
    - from: "tests/conftest.py"
      to: "torch_structured/_ops.py::set_backend"
      via: "torch_structured._ops.set_backend(request.param)"
      pattern: "torch_structured\\._ops\\.set_backend"
    - from: ".github/workflows/test.yml"
      to: "~/.triton/cache"
      via: "actions/cache@v4 path setting"
      pattern: "path: ~/\\.triton/cache"
    - from: "torch_structured/_ops.py::_demo_identity_op"
      to: "torch.library.triton_op + register_autograd + register_fake"
      via: "@triton_op decorator + .register_autograd(...) + .register_fake(...)"
      pattern: "register_fake"
---

<objective>
Land the no-op identity demonstrator op (`_demo_identity_op`) wired through the canonical `@torch.library.triton_op` + `wrap_triton` + `register_autograd` + `register_fake` pipeline inside `torch_structured/_ops.py`, write the five-test acceptance suite at `tests/test_dispatch.py` (eager-fp32, eager-complex64, gradcheck, torch.compile(fullgraph=True), explicit FakeTensorMode trace reproducing-then-fixing the 260419-p27 dynamo bug), introduce `tests/conftest.py` with the `backend` fixture parametrized on `["torch"]` only (Phase 5+ extends), create the empty `torch_structured/_triton/__init__.py` placeholder package, and add `.github/workflows/test.yml` with `actions/cache@v4` for `~/.triton/cache`.

Purpose: Prove the wrapper pattern survives `torch.compile(fullgraph=True)` and `gradcheck` BEFORE Phases 5-8 copy it into real kernels. Lock in CI cache infrastructure now so Phase 5 doesn't pay first-call JIT cost on every PR (Pitfall 5 in RESEARCH.md, TEST-05). The `register_fake` test is the literal acceptance gate for the 260419-p27 bug fix (per planning context `<verification_pattern>`).

Output: Demonstrator op added to existing `_ops.py`, five-test acceptance suite, pytest backend fixture, CI workflow file, empty `_triton/` placeholder. No real kernel — kernels start landing in Phase 5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-triton-dispatch-infrastructure-foundational-decisions/04-CONTEXT.md
@.planning/phases/04-triton-dispatch-infrastructure-foundational-decisions/04-RESEARCH.md
@.planning/phases/04-triton-dispatch-infrastructure-foundational-decisions/04-PATTERNS.md
@.planning/phases/04-triton-dispatch-infrastructure-foundational-decisions/04-01-PLAN.md
@torch_structured/_ops.py
@tests/test_lru.py
@tests/test_multiply.py

<interfaces>
<!-- Contracts produced by Plan 04-01 (Wave 1) that Plan 04-02 consumes. -->

Plan 04-01 created (these are the call sites this plan uses, NOT redefines):

```python
# From torch_structured/_ops.py (created by Plan 04-01)
butterfly_multiply: Callable | None        # rebound by _resolve / set_backend
hadamard_transform: Callable | None
diag_mult: Callable | None
_BACKEND: str
def _has_triton() -> bool: ...
def _has_cuda_legacy() -> bool: ...
def _resolve(name: str) -> str: ...        # raises ValueError on unknown name
def set_backend(name: str) -> str: ...

# From torch_structured/__init__.py (extended by Plan 04-01)
from ._ops import set_backend
```

Contracts this plan ADDS to those:

```python
# torch_structured/_ops.py — added by Plan 04-02 BELOW the Plan 04-01 resolver block
@triton.heuristics(values={'BLOCK_SIZE': lambda args: ...})
@triton.jit
def _demo_identity_kernel(in_ptr, out_ptr, n_elements, BLOCK_SIZE: tl.constexpr): ...

@triton_op("torch_structured::_demo_identity", mutates_args={})
def _demo_identity_op(x: torch.Tensor) -> torch.Tensor: ...

_demo_identity_op.register_autograd(_backward, setup_context=_setup_context)

@_demo_identity_op.register_fake
def _(x: torch.Tensor) -> torch.Tensor: ...   # THE 260419-p27 FIX

# tests/conftest.py — purely additive (no existing conftest)
@pytest.fixture(params=["torch"])
def backend(request): ...

# tests/test_dispatch.py — five-test acceptance suite
test_demo_identity_eager_fp32
test_demo_identity_eager_complex64
test_demo_identity_gradcheck
test_demo_identity_compile_no_graph_break
test_demo_identity_compile_fake_tensor_trace   # the literal 260419-p27 acceptance gate
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append _demo_identity_op skeleton to _ops.py + create _triton/ placeholder package</name>
  <files>
    torch_structured/_ops.py,
    torch_structured/_triton/__init__.py
  </files>
  <read_first>
    - torch_structured/_ops.py (created by Plan 04-01 — Task 1 APPENDS to this file, does NOT rewrite the resolver section)
    - .planning/phases/04-triton-dispatch-infrastructure-foundational-decisions/04-RESEARCH.md §"Pattern 1: Triton Op Skeleton" (lines 234-292) — the verified API contract for triton_op + wrap_triton + register_autograd + register_fake
    - .planning/phases/04-triton-dispatch-infrastructure-foundational-decisions/04-RESEARCH.md §"Code Examples — Complete demonstrator op" (lines 611-657) — the copy-paste-ready skeleton
    - .planning/phases/04-triton-dispatch-infrastructure-foundational-decisions/04-RESEARCH.md §"Pitfall 1: Forgetting register_fake on the Demonstrator" (lines 542-551) — the failure mode being prevented
    - .planning/phases/04-triton-dispatch-infrastructure-foundational-decisions/04-CONTEXT.md D-12 (mandatory triton_op + register_autograd + register_fake pattern, autograd.Function forbidden), D-13 (demonstrator placement), and D-14 (test coverage requirements)
    - .planning/phases/04-triton-dispatch-infrastructure-foundational-decisions/04-PATTERNS.md §"_ops.py" "CRITICAL idiom to preserve" callout
    - .planning/quick/260419-p27-extend-recurrent-poc-torch-compile-track/260419-p27-SUMMARY.md (the bug being reproduced-then-fixed; literal error string is "The tensor has a non-zero number of elements, but its data is not allocated yet")
  </read_first>
  <action>
    APPEND the demonstrator op section to the existing `torch_structured/_ops.py` (created by Plan 04-01). DO NOT touch the resolver section already in the file. Add a clearly-marked section header comment `# ─── Phase 4 demonstrator op (deleted at start of Phase 5 per D-13) ───────` after the import-time `_resolve()` call at the bottom of the existing file.

    Append, in order:

    1. **Additional imports at the TOP of the file** (immediately after the existing `import logging`, `import os`, `import torch` block): `import triton`, `import triton.language as tl`, `from torch.library import triton_op, wrap_triton`. Place these after the Plan 04-01 imports but BEFORE any code, so the module-top-level `_resolve()` call still has them available.
    2. **Kernel definition** per RESEARCH.md Code Examples lines 623-631:
       - `@triton.heuristics(values={'BLOCK_SIZE': lambda args: triton.next_power_of_2(min(args['n_elements'], 1024))})` decorator above `@triton.jit`
       - `def _demo_identity_kernel(in_ptr, out_ptr, n_elements, BLOCK_SIZE: tl.constexpr):` body computes `pid = tl.program_id(axis=0); block_start = pid * BLOCK_SIZE; offsets = block_start + tl.arange(0, BLOCK_SIZE); mask = offsets < n_elements; x = tl.load(in_ptr + offsets, mask=mask); tl.store(out_ptr + offsets, x, mask=mask)`. Identity copy.
    3. **triton_op wrapper** per RESEARCH.md lines 634-640 and the **complex64 branch (per CHECKER B2 fix and RESOLVED Open Question 1 in 04-RESEARCH.md)** — matches the skeleton from 04-RESEARCH.md §Pattern 3:
       - `@triton_op("torch_structured::_demo_identity", mutates_args={})` decorator
       - `def _demo_identity_op(x: torch.Tensor) -> torch.Tensor:` body MUST include the view_as_real/view_as_complex branch unconditionally (so complex64 is a planned-in feature, NOT a contingency):
         ```
         is_complex = x.is_complex()
         if is_complex:
             assert x.is_contiguous(), "complex input must be contiguous before view_as_real (Pitfall 3 / 04-COMPLEX-LAYOUT.md)"
             x_work = torch.view_as_real(x)        # trailing-2 real view; zero-copy
         else:
             x_work = x
         out_work = torch.empty_like(x_work)
         n_elements = x_work.numel()
         grid = lambda meta: (triton.cdiv(n_elements, meta['BLOCK_SIZE']),)
         wrap_triton(_demo_identity_kernel)[grid](x_work, out_work, n_elements)
         return torch.view_as_complex(out_work.contiguous()) if is_complex else out_work
         ```
       - Rationale: the identity demonstrator doesn't need the 4-FMA complex multiply helper from 04-RESEARCH.md §Pattern 3 (this is the no-op path), but it DOES need to exercise the view_as_real boundary so Phase 7 has a working reference and the test in Task 2 (`test_demo_identity_eager_complex64`) is a planned-in test against a planned-in feature — not contingency code. The kernel itself stays unchanged (operates on the trailing-2 real view).
       - Acceptance criterion for this branch: `_demo_identity_op(complex64_input)` returns a `torch.complex64` tensor that is element-wise equal to the input (dtype preserved across the wrapper boundary).
    4. **setup_context and backward** per RESEARCH.md lines 643-648:
       - `def _setup_context(ctx, inputs, output): pass  # identity op needs nothing saved`
       - `def _backward(ctx, grad): return grad  # d(out)/d(in) = 1`
    5. **Register autograd**: `_demo_identity_op.register_autograd(_backward, setup_context=_setup_context)` per RESEARCH.md line 651.
    6. **Register fake (THE 260419-p27 FIX, MANDATORY per D-12 and RESEARCH.md Pitfall 1)** per RESEARCH.md lines 654-656:
       - `@_demo_identity_op.register_fake` decorator
       - `def _(x: torch.Tensor) -> torch.Tensor: return torch.empty_like(x)`
    7. **Inline section comment** above the demonstrator section explaining: "Demonstrator op (D-13, D-14): proves the triton_op + register_autograd + register_fake pipeline survives torch.compile(fullgraph=True) and gradcheck. Deleted at the start of Phase 5 once diag_mult exercises the same pattern on a real kernel. The register_fake decorator below is the literal 260419-p27 fix — DO NOT remove (RESEARCH.md Pitfall 1)."

    Also create the empty placeholder package `torch_structured/_triton/__init__.py` per RESEARCH.md §"Recommended Project Structure" line 211:
    - Module docstring: `"Triton kernel home. Empty in Phase 4; populated by Phase 5+ kernel ports."`
    - One module-level constant: `HAS_TRITON: bool` resolved via try-import — `try: import triton; HAS_TRITON = True; except ImportError: HAS_TRITON = False`. Wrapped with `# noqa: F401 — exposes Triton package as importable, but contents land in Phase 5+` on the triton import (per CHECKER W5 fix — minor consistency with PATTERNS shared-pattern naming).
    - Three to five lines total. Per 04-PATTERNS.md "_triton/" Notes: do NOT import this from `torch_structured/__init__.py` (Pitfall: eagerly importing _triton loads LLVM). Only `_ops.py._has_triton()` may probe it lazily.

    Closest analogs: 04-RESEARCH.md Pattern 1 (lines 234-292), Code Examples (lines 611-657), `torch_structured/recurrent/__init__.py:1-4` for the empty package init style.
  </action>
  <verify>
    <automated>
    cd /home/claroche/torch-structured &amp;&amp; \
    python -c "
import inspect, sys
# Fresh import
for m in list(sys.modules):
    if m.startswith('torch_structured'):
        del sys.modules[m]
from torch_structured import _ops
# 1. Plan 04-01 resolver state still intact
assert hasattr(_ops, '_resolve'), 'Plan 04-01 _resolve missing (regression)'
assert hasattr(_ops, 'set_backend'), 'Plan 04-01 set_backend missing (regression)'
# 2. Plan 04-02 demonstrator op present
assert hasattr(_ops, '_demo_identity_op'), '_demo_identity_op not exposed'
assert hasattr(_ops, '_demo_identity_kernel'), '_demo_identity_kernel not exposed'
# 3. register_fake actually registered — check the op has a fake_impl set
op = _ops._demo_identity_op
# triton_op creates a CustomOpDef-like wrapper; verify the fake path exists
# (Concrete API: torch.library.opcheck or schema introspection)
import torch
qualname = 'torch_structured::_demo_identity'
# The op must be registered in torch.ops
ns, name = qualname.split('::')
assert hasattr(getattr(torch.ops, ns), name), f'Op {qualname} not registered with torch.ops'
# 4. The backward and setup_context wired
src = inspect.getsource(_ops)
assert 'register_autograd' in src, 'register_autograd call missing from _ops.py'
assert 'register_fake' in src, 'register_fake call missing from _ops.py (THE 260419-p27 fix)'
# 5. _triton/ placeholder package exists
import torch_structured._triton as triton_pkg
assert hasattr(triton_pkg, 'HAS_TRITON'), '_triton/__init__.py must define HAS_TRITON'
assert isinstance(triton_pkg.HAS_TRITON, bool), 'HAS_TRITON must be bool'
print('OK: demonstrator op + _triton placeholder verified')
"
    # Independent check via grep — the register_fake decorator must literally appear
    cd /home/claroche/torch-structured &amp;&amp; \
    [ "$(grep -v '^#' torch_structured/_ops.py | grep -c '@_demo_identity_op.register_fake')" -ge 1 ] &amp;&amp; \
    [ "$(grep -v '^#' torch_structured/_ops.py | grep -c 'register_autograd')" -ge 1 ] &amp;&amp; \
    [ "$(grep -v '^#' torch_structured/_ops.py | grep -c 'wrap_triton')" -ge 1 ] &amp;&amp; \
    [ "$(grep -v '^#' torch_structured/_ops.py | grep -c '@triton_op')" -ge 1 ] &amp;&amp; \
    echo "OK: triton_op + wrap_triton + register_autograd + register_fake all present (filtered)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from torch_structured._ops import _demo_identity_op; print(_demo_identity_op)"` exits 0
    - `torch_structured/_ops.py` (filtered through `grep -v '^#'` to ignore comments) contains all four literal decorator/method calls: `@triton_op(`, `wrap_triton`, `register_autograd`, `@_demo_identity_op.register_fake` (this is the literal 260419-p27 fix per RESEARCH.md Pitfall 1; missing this line means the test in Task 2 will fail with the original bug)
    - The op is registered with the namespace `torch_structured::_demo_identity` (verified via `torch.ops.torch_structured._demo_identity` exists)
    - Plan 04-01 artifacts (`_resolve`, `set_backend`, `_BACKEND`, `butterfly_multiply` rebinding) still function correctly — verify via `python -c "import torch_structured; torch_structured.set_backend('torch'); assert torch_structured._ops._BACKEND == 'torch'"` exits 0
    - `torch_structured/_triton/__init__.py` exists with a `HAS_TRITON: bool` module-level constant
    - `torch_structured/_triton/__init__.py` does NOT trigger an LLVM/Triton import side-effect that propagates to `torch_structured/__init__.py` (verify: `python -c "import sys; import torch_structured; assert 'torch_structured._triton' not in sys.modules"` succeeds — Phase 4 does not eagerly import _triton; only the lazy probe in `_ops.py._has_triton()` does it)
    - The kernel decorator order is `@triton.heuristics(...)` ABOVE `@triton.jit` (per RESEARCH.md A4 — verified order)
    - **Complex64 wrapper boundary (per CHECKER B2 fix / RESOLVED Open Question 1):** `_demo_identity_op(x)` where `x.dtype == torch.complex64` returns a `torch.complex64` tensor (dtype preserved across the view_as_real → kernel → view_as_complex round-trip). The function source contains both `view_as_real` and `view_as_complex` literal calls.
  </acceptance_criteria>
  <done>The demonstrator op is registered with PyTorch's library subsystem via the full `triton_op + register_autograd + register_fake` pipeline. The `_triton/` placeholder package exists for Phase 5+ kernel imports. Task 2 can now write the five-test acceptance suite against `torch_structured._ops._demo_identity_op`.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/conftest.py + tests/test_dispatch.py (five-test acceptance suite including the 260419-p27 acceptance gate)</name>
  <files>
    tests/conftest.py,
    tests/test_dispatch.py
  </files>
  <read_first>
    - torch_structured/_ops.py (Task 1 just added the demonstrator; tests import `_demo_identity_op` from here and call `set_backend` via the `backend` fixture)
    - .planning/phases/04-triton-dispatch-infrastructure-foundational-decisions/04-RESEARCH.md §"`conftest.py` with `backend` fixture (Plan 2)" (lines 789-807) — verbatim conftest skeleton
    - .planning/phases/04-triton-dispatch-infrastructure-foundational-decisions/04-RESEARCH.md §"Code Examples — Complete test for the demonstrator" (lines 659-714) — the five test bodies verbatim
    - .planning/phases/04-triton-dispatch-infrastructure-foundational-decisions/04-CONTEXT.md D-14 — three required test cases (compile/no-graph-break, gradcheck, fake-tensor reproducer)
    - .planning/phases/04-triton-dispatch-infrastructure-foundational-decisions/04-PATTERNS.md §"tests/conftest.py" and §"tests/test_dispatch.py" — analog imports and pytest-style allowance for new files
    - tests/test_lru.py:1-12 (import-style anchor: `import torch_structured  # noqa: F401 — triggers extension load`)
    - tests/test_multiply.py:1-12 (existing unittest.TestCase style — for context; test_dispatch.py deviates to pytest per RESEARCH.md Standard Stack Alternatives)
    - .planning/quick/260419-p27-extend-recurrent-poc-torch-compile-track/260419-p27-SUMMARY.md (the bug literal: error message string is "The tensor has a non-zero number of elements, but its data is not allocated yet")
  </read_first>
  <action>
    Write two test files using the verbatim RESEARCH.md skeletons. The five tests in `test_dispatch.py` are the acceptance gate for Phase 4: if `test_demo_identity_compile_fake_tensor_trace` fails with the literal 260419-p27 error string, Phase 4 has not shipped its promised fix.

    `tests/conftest.py` per RESEARCH.md lines 789-807:
    - Module docstring: "Phase 4: backend fixture parametrized over ['torch'] only. Phase 5+ will extend to ['torch', 'triton', 'cuda'] as kernels land."
    - `import pytest`
    - `import torch_structured  # noqa: F401 — triggers extension load + _ops.py resolver` (use the `tests/test_lru.py:6` comment style per 04-PATTERNS.md "Module-import-as-side-effect comment" shared pattern)
    - Fixture: `@pytest.fixture(params=["torch"])`, `def backend(request):`, docstring `"Switch backend for the duration of a test, restore after."`, body: capture `original = torch_structured._ops._BACKEND`, `chosen = torch_structured._ops.set_backend(request.param)`, `yield chosen`, then in teardown `torch_structured._ops.set_backend(original)`.

    `tests/test_dispatch.py` per RESEARCH.md lines 661-714 — write exactly the five tests below. Each test name MUST match the verify gates in Task 2 acceptance criteria.

    1. **Imports**: `import pytest`, `import torch`, `from torch_structured._ops import _demo_identity_op`. Top-level skipif: `pytestmark = pytest.mark.skipif(not torch.cuda.is_available(), reason="demo op is GPU-only")` per RESEARCH.md line 670.
    2. `def test_demo_identity_eager_fp32():` — `x = torch.randn(128, device='cuda', requires_grad=True); y = _demo_identity_op(x); assert torch.equal(y, x)`.
    3. `def test_demo_identity_eager_complex64():` — `x = torch.randn(128, dtype=torch.complex64, device='cuda', requires_grad=True); y = _demo_identity_op(x); assert torch.equal(y, x); assert y.dtype == torch.complex64`. Docstring noting "Exercises the view_as_real/view_as_complex path through the wrapper boundary (D-14 recommendation; planned-in feature per CHECKER B2 fix and RESOLVED Open Question 1 in 04-RESEARCH.md)." This is a **required, planned-in test against a planned-in feature** — its acceptance criterion is simply "passes with no skip/xfail" (the wrapper boundary from Task 1's `_demo_identity_op` makes it work unconditionally). No contingency expansion path — Task 1 already includes the complex branch.
    4. `def test_demo_identity_gradcheck():` — `x = torch.randn(16, dtype=torch.float64, device='cuda', requires_grad=True); assert torch.autograd.gradcheck(_demo_identity_op, (x,), eps=1e-6, atol=1e-5)`. Per D-14b.
    5. `def test_demo_identity_compile_no_graph_break():` — docstring `"The 260419-p27 fix verification: register_fake must let dynamo trace through."` Use `@torch.compile(fullgraph=True)` (fullgraph raises on graph break — this is the strict gate per D-14a). Function `def f(x): return _demo_identity_op(x) * 2`. Then `x = torch.randn(128, device='cuda', requires_grad=True); y = f(x); assert torch.allclose(y, x * 2)`.
    6. `def test_demo_identity_compile_fake_tensor_trace():` — docstring `"Explicit fake-tensor trace — the literal 260419-p27 reproducer."` THIS IS THE BLOCKING ACCEPTANCE GATE per planning context `<verification_pattern>`. Body: `from torch._subclasses.fake_tensor import FakeTensorMode; with FakeTensorMode(): x = torch.empty(128, device='cuda', requires_grad=True); y = _demo_identity_op(x); assert y.shape == x.shape; assert y.dtype == x.dtype`. The assertion is that no `"The tensor has a non-zero number of elements, but its data is not allocated yet"` error is raised. The `register_fake` decorator in Task 1 is what makes this test pass — without it, this test reproduces the original bug.

    No additional tests in Phase 4 — Plan 04-02's job is to ship the five canonical acceptance tests, not a comprehensive suite (Phase 5+ extends).

    Closest analogs per 04-PATTERNS.md: RESEARCH.md lines 661-714 (test skeleton verbatim), RESEARCH.md lines 791-807 (conftest verbatim), tests/test_lru.py:1-12 (import-side-effect comment style).
  </action>
  <verify>
    <automated>
    cd /home/claroche/torch-structured &amp;&amp; \
    test -f tests/conftest.py &amp;&amp; test -f tests/test_dispatch.py &amp;&amp; \
    grep -c 'def backend(' tests/conftest.py | tr -d ' '
    # Expect: 1
    cd /home/claroche/torch-structured &amp;&amp; \
    [ "$(grep -v '^#' tests/test_dispatch.py | grep -c 'def test_demo_identity_eager_fp32')" -ge 1 ] &amp;&amp; \
    [ "$(grep -v '^#' tests/test_dispatch.py | grep -c 'def test_demo_identity_eager_complex64')" -ge 1 ] &amp;&amp; \
    [ "$(grep -v '^#' tests/test_dispatch.py | grep -c 'def test_demo_identity_gradcheck')" -ge 1 ] &amp;&amp; \
    [ "$(grep -v '^#' tests/test_dispatch.py | grep -c 'def test_demo_identity_compile_no_graph_break')" -ge 1 ] &amp;&amp; \
    [ "$(grep -v '^#' tests/test_dispatch.py | grep -c 'def test_demo_identity_compile_fake_tensor_trace')" -ge 1 ] &amp;&amp; \
    [ "$(grep -v '^#' tests/test_dispatch.py | grep -c 'FakeTensorMode')" -ge 1 ] &amp;&amp; \
    [ "$(grep -v '^#' tests/test_dispatch.py | grep -c 'fullgraph=True')" -ge 1 ] &amp;&amp; \
    [ "$(grep -v '^#' tests/test_dispatch.py | grep -c 'gradcheck')" -ge 1 ] &amp;&amp; \
    echo "OK: test_dispatch.py contains all five required tests"
    cd /home/claroche/torch-structured &amp;&amp; \
    python -m pytest tests/test_dispatch.py --collect-only -q 2>&amp;1 | grep -E 'test_demo_identity_(eager_fp32|eager_complex64|gradcheck|compile_no_graph_break|compile_fake_tensor_trace)' | wc -l | xargs -I{} test {} -ge 5 &amp;&amp; \
    echo "OK: pytest collects all 5 tests"
    # Run-time gate: if CUDA available, all 5 tests pass. If no CUDA, they skip cleanly (pytestmark).
    cd /home/claroche/torch-structured &amp;&amp; \
    python -m pytest tests/test_dispatch.py -x --no-header -q 2>&amp;1 | tail -30
    # The literal acceptance gate: on a CUDA machine, test_demo_identity_compile_fake_tensor_trace MUST exit 0
    # (no "The tensor has a non-zero number of elements, but its data is not allocated yet")
    </automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` exists with the `backend` fixture parametrized on `["torch"]` (NOT on `["torch","triton","cuda"]` — that's Phase 5+ scope)
    - `tests/conftest.py` `backend` fixture restores the original `_BACKEND` after teardown (yield-based cleanup)
    - `tests/test_dispatch.py` exists with EXACTLY the five required test functions named: `test_demo_identity_eager_fp32`, `test_demo_identity_eager_complex64`, `test_demo_identity_gradcheck`, `test_demo_identity_compile_no_graph_break`, `test_demo_identity_compile_fake_tensor_trace`. (Verified by `grep -v '^#'` then `grep -c 'def test_demo_identity_<name>'`.)
    - `tests/test_dispatch.py` uses `pytestmark = pytest.mark.skipif(not torch.cuda.is_available(), reason="demo op is GPU-only")` so the suite skips cleanly on CPU-only environments
    - `tests/test_dispatch.py` `test_demo_identity_compile_no_graph_break` uses `@torch.compile(fullgraph=True)` (per D-14a — `fullgraph=True` raises on graph break, the strict gate)
    - `tests/test_dispatch.py` `test_demo_identity_compile_fake_tensor_trace` uses `FakeTensorMode` explicitly — this is THE 260419-p27 acceptance gate per `<verification_pattern>` in the planning context
    - `python -m pytest tests/test_dispatch.py --collect-only` collects all 5 tests without errors
    - On a CUDA-capable runner: `tests/test_dispatch.py::test_demo_identity_compile_fake_tensor_trace` exits 0 (NO "The tensor has a non-zero number of elements, but its data is not allocated yet" error). This is the blocking gate. On CPU-only runners: all 5 tests skip cleanly via the pytestmark.
    - `pytest tests/test_dispatch.py` runs at least the import + collect path without errors EVEN on CPU-only machines (the pytestmark skipif must trigger BEFORE the import of CUDA-dependent code)
  </acceptance_criteria>
  <done>The five-test acceptance suite is in place. On CUDA hardware, all five tests pass — proving the wrapper pattern works end-to-end through compile, gradcheck, and fake-tensor tracing. The `register_fake` decorator added in Task 1 is the literal fix for the 260419-p27 dynamo bug, and `test_demo_identity_compile_fake_tensor_trace` is the regression gate for it. Phase 5 will extend the `backend` fixture parametrization to `["torch", "triton"]` when the first real kernel lands.</done>
</task>

<task type="auto">
  <name>Task 3: Create .github/workflows/test.yml with actions/cache@v4 for Triton JIT cache (TEST-05)</name>
  <files>
    .github/workflows/test.yml
  </files>
  <read_first>
    - .planning/phases/04-triton-dispatch-infrastructure-foundational-decisions/04-RESEARCH.md §"Pattern 5: GitHub Actions Cache for `~/.triton/cache`" (lines 453-500) — the verbatim workflow YAML template
    - .planning/phases/04-triton-dispatch-infrastructure-foundational-decisions/04-RESEARCH.md §"Pitfall 6: CI Cache Hit Rate is 0% Because Key Includes Random Hashes" (lines 599-605) — DO NOT key on github.sha; use hashFiles for source-hash component
    - .planning/phases/04-triton-dispatch-infrastructure-foundational-decisions/04-CONTEXT.md D-16 (actions/cache@v4 explicit pin, key includes torch.__version__ and git SHA of `_triton/` directory)
    - .planning/phases/04-triton-dispatch-infrastructure-foundational-decisions/04-PATTERNS.md §".github/workflows/test.yml" — no in-repo analog (the repo has no .github yet); use RESEARCH.md Pattern 5 verbatim
    - pyproject.toml (confirm the test extra `[project.optional-dependencies]` `test = ["pytest"]` exists — the workflow installs via `pip install -e .[test]`)
  </read_first>
  <action>
    Create `.github/workflows/test.yml` from the verbatim RESEARCH.md Pattern 5 template (lines 466-487). This is the first CI workflow in the repo (no `.github/` directory exists per 04-PATTERNS.md "_github/workflows_/test.yml" Notes); the file establishes the convention Phase 5+ will extend.

    YAML structure (apply RESEARCH.md Pattern 5 lines 466-487 — pinned to `actions/cache@v4` per D-16):

    1. `name: Test`
    2. `on: [push, pull_request]`
    3. `jobs.test` running on `ubuntu-latest`
    4. Steps in order:
       - `actions/checkout@v4`
       - `actions/setup-python@v5` with `python-version: '3.11'` (matches `requires-python = ">=3.10"` in pyproject; 3.11 is the de facto LTS choice for PyTorch 2.6+ runners)
       - `name: Cache Triton JIT artifacts` step using `actions/cache@v4` (per D-16 — NOT v3 or v5):
         - `path: ~/.triton/cache`
         - `key: triton-${{ runner.os }}-py${{ env.PYTHON_VERSION }}-torch${{ env.TORCH_VERSION }}-${{ hashFiles('torch_structured/_triton/**/*.py') }}` (per RESEARCH.md Pattern 5 line 481 — `hashFiles` for source-hash component per Pitfall 6, NOT `github.sha`)
         - `restore-keys: |` with two fallback prefixes:
           - `triton-${{ runner.os }}-py${{ env.PYTHON_VERSION }}-torch${{ env.TORCH_VERSION }}-`
           - `triton-${{ runner.os }}-py${{ env.PYTHON_VERSION }}-`
       - `- run: pip install -e .[test]` (consumes the `test` extra defined in pyproject.toml's `[project.optional-dependencies]`)
       - `- run: pytest tests/ -x`

    Phase 4 reality check (per RESEARCH.md line 500): Phase 4 has NO real Triton kernels, so the demonstrator's compile cost is trivial. The cache infrastructure exists in Phase 4 because (a) it's the TEST-05 requirement, (b) Phase 5 will immediately exercise it with the first real kernel. Empty `_triton/` directory means `hashFiles('torch_structured/_triton/**/*.py')` returns a stable empty-glob hash in Phase 4; the cache key effectively reduces to `(os, python, torch)` until Phase 5 adds kernel files.

    Closest analog: 04-RESEARCH.md Pattern 5 (lines 466-487) — no in-repo analog (no `.github/` directory existed before this plan).
  </action>
  <verify>
    <automated>
    cd /home/claroche/torch-structured &amp;&amp; \
    test -f .github/workflows/test.yml &amp;&amp; \
    [ "$(grep -v '^#' .github/workflows/test.yml | grep -c 'actions/cache@v4')" -ge 1 ] &amp;&amp; \
    [ "$(grep -v '^#' .github/workflows/test.yml | grep -c 'path: ~/.triton/cache')" -ge 1 ] &amp;&amp; \
    [ "$(grep -v '^#' .github/workflows/test.yml | grep -c 'restore-keys:')" -ge 1 ] &amp;&amp; \
    [ "$(grep -v '^#' .github/workflows/test.yml | grep -c 'pip install -e .\[test\]')" -ge 1 ] &amp;&amp; \
    [ "$(grep -v '^#' .github/workflows/test.yml | grep -c 'pytest tests/')" -ge 1 ] &amp;&amp; \
    echo "OK: workflow YAML has all required gates"
    # Pitfall 6 negative check: must NOT key on github.sha (filtered, so the test still works if comments reference Pitfall 6)
    cd /home/claroche/torch-structured &amp;&amp; \
    [ "$(grep -v '^#' .github/workflows/test.yml | grep -c 'github.sha')" -eq 0 ] &amp;&amp; \
    echo "OK: workflow does NOT key on github.sha (Pitfall 6 avoided)"
    # hashFiles glob for source-hash invalidation component
    cd /home/claroche/torch-structured &amp;&amp; \
    [ "$(grep -v '^#' .github/workflows/test.yml | grep -c \"hashFiles('torch_structured/_triton/\\*\\*/\\*.py')\")" -ge 1 ] &amp;&amp; \
    echo "OK: hashFiles on _triton sources present"
    # YAML lint via Python (no external dep needed for basic syntax check)
    cd /home/claroche/torch-structured &amp;&amp; \
    python -c "import yaml; yaml.safe_load(open('.github/workflows/test.yml'))" &amp;&amp; \
    echo "OK: test.yml is valid YAML"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/test.yml` exists at the repo root in a new `.github/workflows/` directory
    - The workflow uses `actions/cache@v4` literally (NOT v3 or v5, per D-16)
    - The cache `path:` is `~/.triton/cache` literally
    - The cache `key:` includes `runner.os`, a python version reference, a torch version reference, and `hashFiles('torch_structured/_triton/**/*.py')` (filtered through `grep -v '^#'` so comments referencing v3/v5 don't trip the gate)
    - The cache `key:` does NOT contain `github.sha` (Pitfall 6 — keying on commit SHA yields 0% cache hit rate)
    - The workflow has `restore-keys` with at least two fallback prefixes (graceful fallback on partial matches per RESEARCH.md Pattern 5)
    - The workflow installs via `pip install -e .[test]` and runs `pytest tests/ -x`
    - `python -c "import yaml; yaml.safe_load(open('.github/workflows/test.yml'))"` exits 0 (the YAML is syntactically valid; this is a cheap gate without requiring a real CI run)
  </acceptance_criteria>
  <done>The repo now has its first CI workflow, with `~/.triton/cache` persisted between runs per TEST-05. The cache key is keyed on stable axes (OS, Python, torch version, Triton source-hash) so it has a non-zero hit rate across PRs that don't touch Triton kernel sources. Phase 5 inherits a working cache the moment the first real kernel lands. Pitfall 6 (commit-SHA keying) is explicitly avoided.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process env -> Triton | `TRITON_CACHE_DIR` (defaults to `~/.triton/cache`) crosses from CI runner env into Triton's JIT compiler. Phase 4 does not override this. |
| Disk cache -> Triton | The `~/.triton/cache` directory contains TTIR/TTGIR/LLIR/PTX/CUBIN — compiled GPU code. CI persists this between runs via `actions/cache@v4`. |
| GitHub Actions runner -> repo code | Tests run `_demo_identity_op` through `torch.compile`, which compiles via Triton, which writes to the cache directory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | T (Tampering) | Triton JIT cache poisoning via `actions/cache@v4` | accept | Triton's cache is per-repo, per-key, scoped to the GitHub Actions runner cache namespace. A poisoned cache would have to come from a write within our own CI — i.e., an attacker already with commit-and-push capability. The cache key includes a `hashFiles` source-hash component (per Pitfall 6 mitigation), so commits that modify `torch_structured/_triton/**/*.py` invalidate prior cache entries. Risk is local (per-fork CI), low-value (no secrets in compiled kernels), and the CI cache key composition is the standard `actions/cache@v4` template. No mitigation needed beyond the existing `hashFiles` invalidation. |
| T-04-02-02 | T (Tampering) | The demonstrator op `_demo_identity_op` is a no-op identity; no kernel logic to attack | accept | The kernel does `tl.load(in_ptr); tl.store(out_ptr)` — no arithmetic, no branching, no memory layout choices. The threat surface is trivially small. Phase 5+ will add real kernels and re-evaluate per-kernel; Phase 4 demonstrator is out of scope. |
| T-04-02-03 | D (DoS) | `~/.triton/cache` growing unbounded in CI | accept | GitHub Actions cache has a 10 GB per-repo limit and auto-evicts oldest entries. Triton cache entries are <1 MB each; thousands of distinct kernel-shape entries fit comfortably. No mitigation needed. RESEARCH.md Pattern 5 verifies. |
| T-04-02-04 | I (Information Disclosure) | Logging output from `DISP-05` info messages leaks runtime backend selection | accept | Same as Plan 04-01 T-04-02. No PII, no paths, no secrets — just the backend name. |

## No High-Severity Threats

Plan 04-02 adds: (1) an in-process Python function (`_demo_identity_op`) that performs a no-op identity copy on a tensor (no user input crosses a network boundary, no auth, no DB), (2) pytest tests (test-only code, no production runtime effect), (3) an empty `_triton/` placeholder package, (4) a GitHub Actions workflow file (CI infra, no secrets, no deploy keys, no external services). No ASVS L1 categories apply (no auth, no session, no access control, no validation/encoding for HTTP, no cryptography surface, no error/logging beyond INFO, no data protection — there is no data, no communications surface, no malicious code execution path, no business logic, no file/resource surface beyond `~/.triton/cache` which is documented and standard, no API to attack, configuration is checked into git and reviewed). All threats above are accepted as low-severity.
</threat_model>

<verification>
Plan-level verification (run after all three tasks complete):

1. Demonstrator op registered: `python -c "import torch; import torch_structured; print(torch.ops.torch_structured._demo_identity)"` exits 0.
2. Five tests collected: `python -m pytest tests/test_dispatch.py --collect-only -q | grep -c 'test_demo_identity'` returns at least 5.
3. On CUDA hardware: `python -m pytest tests/test_dispatch.py -x` exits 0 with five passes. (On CPU: cleanly skips via `pytestmark`.)
4. THE acceptance gate (per `<verification_pattern>`): `pytest tests/test_dispatch.py::test_demo_identity_compile_fake_tensor_trace -x` exits 0 on CUDA. The error string "The tensor has a non-zero number of elements, but its data is not allocated yet" does NOT appear in the output.
5. CI YAML is syntactically valid: `python -c "import yaml; yaml.safe_load(open('.github/workflows/test.yml'))"` exits 0.
6. Plan 04-01 invariants preserved (no regression): `python -c "import torch_structured; torch_structured.set_backend('torch'); assert torch_structured._ops._BACKEND == 'torch'"` exits 0.
</verification>

<success_criteria>
- `_demo_identity_op` is registered via `@torch.library.triton_op` + `wrap_triton` + `register_autograd` + `register_fake` in `torch_structured/_ops.py` (TRI-05, D-12, D-13)
- The literal acceptance gate `test_demo_identity_compile_fake_tensor_trace` passes on CUDA — the 260419-p27 dynamo bug does NOT reproduce because `register_fake` is registered (per `<verification_pattern>` in planning context)
- All five required test functions exist in `tests/test_dispatch.py` (D-14a + b + c + complex64 + eager-fp32)
- `tests/conftest.py` provides a `backend` fixture parametrized on `["torch"]` (Phase 5+ extends)
- `torch_structured/_triton/__init__.py` exists as an empty placeholder package
- `.github/workflows/test.yml` exists with `actions/cache@v4` keyed on `(os, python, torch, hashFiles('torch_structured/_triton/**/*.py'))` — NOT keyed on `github.sha` (TEST-05, D-16)
- `pip install -e .[test]` is the install command in CI; `pytest tests/ -x` is the test command
- Plan 04-01 invariants (set_backend, _BACKEND, butterfly_multiply rebinding, ValueError on unknown backend) all still pass — Plan 04-02 does NOT regress the resolver
</success_criteria>

<output>
After completion, create `.planning/phases/04-triton-dispatch-infrastructure-foundational-decisions/04-02-SUMMARY.md` describing:
- The demonstrator op skeleton at `torch_structured/_ops.py::_demo_identity_op` and the four decorator/method calls that compose it
- The five tests in `tests/test_dispatch.py` and which D-XX decision each one verifies
- The `backend` fixture in `tests/conftest.py` and the Phase 5+ extension point (extend `params=` list)
- The `.github/workflows/test.yml` cache key composition and Pitfall 6 avoidance
- The 260419-p27 acceptance gate result (PASS / SKIP with reason)
- Note that Phase 5 begins by (a) deleting `_demo_identity_op` per D-13 and (b) extending the `backend` fixture params to include `"triton"` (and eventually `"cuda"`)
</output>
